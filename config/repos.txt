# FR-003：这些仓库的当日 git 提交会被采集进学习记忆。
#
# 格式：一行一个仓库路径；`#` 开头是注释；空行忽略。
# 路径必须是**绝对路径** —— collect._git_materials 不做 `~` 展开，
# 写成 ~/Code/... 会原样传给 `git -C`，然后记一条 warning 跳过（NFR-004），
# 看起来"跑了"其实什么都没采。
#
# 仓库不存在 / git 失败只跳过该仓库，不会中断当日 sync。
/Users/nava/Code/llm/rag/first-rag
