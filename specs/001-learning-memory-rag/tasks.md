# Tasks: first-rag v0.1

**Input**: plan.md / spec.md / data-model.md / contracts/plugin-contract.md（**当前对齐 PRD v0.7**；T001–T045 为 v0.1 范围，T046–T058 为 v0.7 新增的 Phase 9）
**组织方式**: 按 plan 纵切里程碑分 Phase（宪法 VII / LG-002），任务标签 `[USx]` 映射 spec 用户故事
**★ 标注**: 默认用户手写任务（宪法 VII v2.0.0），AI 仅 review 答疑；T004/T012/T015/T017 已获用户 2026-09-20、T027/T037 已获用户 2026-09-24 逐任务明确授权由 AI 实现，其余可 AI 生成、用户抽查

## Phase 1: Setup（M0 脚手架）

- [x] T001 [P][US1] 项目脚手架：docker-compose.yml（仅 qdrant，卷 `./data/qdrant/`）、`.env.example`、`.gitignore`（**初版必含 `data/` `.env` `notes/`**，PRD §6 规则 2）、`pyproject.toml`、`Makefile` 骨架。验证：`make up` 后 Qdrant dashboard 可访问（AC-001 前半）——*代码就位，`make up` 待与 T005 一并真跑*
- [x] T002 [P][US1] `src/config.py`：环境变量加载（.env）、路径常量、时区 Asia/Shanghai。验证：单测读 fixture .env（FR-015、§6）——*单测过（.env fixture 读取 + 缺失报错）*
- [x] T003 [P][US1] `config/schema.json` 初版 + 加载校验：types / distill / retrieval / trae_type_map / raw_retention_days，结构按 data-model.md。验证：改坏结构时报错明确（FR-007/016/019/020）——*含 5 个负向单测（缺字段/空 types/坏 mode 等）*
- [x] T004 ★[US1] `src/ark_client.py`：`embed(texts)` / `chat(messages, json_mode)`，openai SDK 指向 Ark base_url（FR-011 密钥走环境）——*用户于 2026-09-20 明确授权 AI 实现（宪法 v2.0.0）；5 个 fake-client 单测覆盖环境配置、空输入、批量顺序、数量校验、JSON mode*
- [x] T005 [P][US1] `make embed-test`：真调验证候选嵌入模型 ID → 确认维度 → 写回 `.env` → 动态维度建集合脚本（AC-001；PRD §9 假设验证点）——*`scripts/embed_test.py` 就位（含 F1 维度冲突检测）；**2026-09-25 真跑通过**：EMBED_MODEL=doubao-embedding-vision 真调返回 2048 维向量，集合 `learning_memory` 已存在且维度匹配（Cosine），脚本判 AC-001 成立*
- [x] T006 [P][US1] `tests/` 骨架 + pytest 配置（unit/integration 分层，fixture 全用系统 tmp——宪法 V）——*conftest tmp_data_dir + test_config.py 7 用例*

**Checkpoint**: `make up` + `make embed-test` 全绿，基础设施就绪。

## Phase 2: Foundation（M1 前置）

- [x] T007 [US1] `src/plugins/__init__.py`：registry（扫描含 `PLUGIN` 的包注册；导入失败/目录缺失 → 静默跳过 + 日志）+ `SourceRef`/`RawMaterial` 类型定义（FR-001；F3 修复）
- [x] T008 [P][US1] `src/plugins/_template/`：插件模板（discover/parse 两函数 + 说明）（US-6；AC-011 插件面）
- [x] T009 [P][US1] 单测：registry 静默跳过行为（NFR-004；AC-006 前置）——*5 用例：正常注册/坏导入不炸/缺 PLUGIN 记日志/_template 被忽略*

**Checkpoint**: 插件契约可用，registry 行为有测试兜底。

## Phase 3: US1 每日自动沉淀（M1 采集直插 → M2 蒸馏 → M6 插件扩展）

**Independent Test**: 当日真实素材 `make sync` 后库中可查（AC-002）

- [x] T010 [US1] `src/plugins/claude_code/`：按 research.md 格式解析 `~/.claude/projects/**.jsonl`，提取消息/工具报错/struggle 轮次 meta（FR-002；源目录只读——宪法 V）——*6 用例全过（含 struggle 连续计数、跨日过滤）*——*v2 增强：~/.claude 存在但 projects/ 缺失时输出 layout drift 警告（本机实测触发）；summary 行忽略锁定为回归测试*
- [x] T011 [US1] `src/collect.py`：汇聚各插件 discover/parse + git（`config/repos.txt`，FR-003）+ 快记 → `data/raw/YYYY-MM-DD.json`（含蒸馏运行元信息，FR-006）；空日写空快照（§8）。验证：repos.txt 缺失/路径失效 → no-op + 日志（G3 补）——*7 用例全过*
- [x] T012 ★[US1] `src/ids.py`：uuid5(source|date|content_hash)，含 content_hash 定义（FR-014；NFR-003）——*用户于 2026-09-20 明确授权 AI 实现（宪法 v2.0.0）；5 个单测覆盖 sha256 前 16 位、UUIDv5 公式、确定性、身份变化、空输入拒绝*
- [x] T013 [US1] `src/ingest.py` 直插路径：embed → upsert（payload 按 data-model.md 全字段）（依赖 T004/T012）——*5 个 fake-client/fake-embed 单测覆盖 Entry 全字段、空输入 no-op、批量 embedding 顺序、确定性 ID、完整 payload、同批去重；真实 Qdrant 集成待 T014*
- [x] T014 [US1] integration 测试：同日重跑幂等——points 数不变（AC-003；FR-014）——*真实 Qdrant 测试通过：fake embedding 下同日重跑保持 2 个 points，ID 命中 UUIDv5 期望值；测试客户端以 trust_env=False 隔离宿主 SOCKS 代理*
- [x] T015 ★[US1] `src/distill_prompt.py`：**用户手写（本次授权 AI 实现）**——rubric → prompt 运行时拼装（类型枚举来自 config；include/exclude 信号、keep/drop 示例注入）（FR-007）——*用户于 2026-09-20 明确授权 AI 实现；6 个单测覆盖配置 rubric/type 注入、严格 JSON 契约、untrusted data 边界、素材溯源、system+user 顺序、空素材；好/坏例子见 example/*
- [x] T016 [US1] `src/distill.py` 编排：脱敏正则（FR-011）→ LLM → JSON 解析 → 未知类型重试一次后丢弃（FR-012）→ 熔断（FR-010）→ pre-summarized 快记/trae 直并入（FR-013）——*10 个核心单测覆盖直接路径、脱敏+快照、合法 JSON、非法 JSON 重试、未知类型重试/丢弃、熔断、空素材；好/坏例子见 example/*
- [x] T017 ★[US1] `src/similarity.py` 新颖度去重：**用户手写（本次授权 AI 实现）**——候选嵌入 → 检索已有 → 超阈值跳过/并入（FR-009）——*12 个 similarity 单测 + 2 个 ingest 接线测试覆盖 collection 缺失、阈值边界、同 ID 重跑、跳过语义重复；好/坏例子见 example/*
- [x] T018 [US1] 单测：脱敏（伪造密钥 fixture，AC-012）、未知类型（AC-013）、熔断（临时上限 3，AC-014）——*6 个 AC 专项用例（tests/unit/test_distill_ac.py）：AC-012 双路径（快记直并入 + LLM）8 类伪造密钥全链路不泄漏、AC-013 重试一次后丢弃其余保留/可纠正恢复、AC-014 恰好上限无熔断日志/超限截断留日志正常结束*
  - **2026-09-24 更正：AC-012 当初的"8 类伪造密钥"里没有本项目真实用的那一种。** 脱敏的令牌名单只有 `sk-`/`ghp_`/`github_pat_`/`AKIA`/JWT，而 Ark 的密钥是 `ark-<UUID>`；赋值式规则又以 `\b` 起头，`_` 属于单词字符，所以 `ARK_API_KEY=…` 整条不匹配（`X-API-Key:` 反而能中，因为连字符给得出边界）。也就是说"进 LLM 前已脱敏"（宪法 V / NFR-001）对我们实际持有的那把钥匙是空话，而测试全绿是因为夹具恰好只喂 `sk-test-` 形态。已补 `ark-`（含 UUID 带连字符的形态）与键名前缀匹配，并把两种 ark 形态喂进 AC-012 的双路径素材；`test_sanitize.py` 同时钉住反向误伤（`task-lifecycle.ts` / `landmark-2` 必须原样通过）。
- [x] T019 [US1] `src/sync.py`：串联 collect→distill→ingest + `data/.sync.lock` 文件锁 + 到期 raw 清理（FR-022；F2 修复）+ `make sync [D=]` 入口（FR-025）——*18 个单测（tests/unit/test_sync.py）：串联顺序/默认今日、跨平台文件锁（fcntl/msvcrt）运行期互斥+崩溃后释放、retention 90/0/null 三态、scope.json 透传给 gather、CLI D=/裸日期/非法日期/失败非零码；`make sync` 入口就位*
- [x] T021 [P][US1] `src/plugins/codex/`：按 research.md 解析 rollout-*.jsonl（FR-002）——*7 个单测（tests/unit/test_codex.py）覆盖本地日判定（UTC 目录名≠归属日）、developer 角色丢弃、task_complete/turn_aborted 报错与 struggle 计数、AGENTS.md/<skill> 注入文本过滤；已用本机真实 ~/.codex 数据只读冒烟验证（8 会话/日，转写以真实用户发言开头）；**v2 增强（同日）**：工具级报错提取（function/custom_tool_call_output 的 str 与内容块列表两种形态，Exit code: N≠0 / execution error / Script failed，成功输出不进转写只用于挣扎归零）、struggle 语义修正（assistant 叙述不打断失败连击，仅工具成功归零）、agent_message 实测为子代理通信锁定跳过；真实数据复测：2026-08-21 单日提取 79 个错误（旧版仅任务级 8/14 全天 8 个）*
- [x] T022 [P][US1] `src/plugins/kimi_code/`：解析 wd_*/session_*/wire.jsonl（FR-002）——*7 个单测（tests/unit/test_kimi_code.py）覆盖三种 wire 事件形态（flat role/claude 块/typed）、state.json 缺失容错、报错与连续 struggle 计数、本地日判定；**2026-09-25 更新：真实数据已到位，"容错假设"已验证** —— 本机 `~/.kimi-code/sessions/` 现有 111 个 `wire.jsonl`，真实层级是 `wd_*/session_*/agents/<agent>/wire.jsonl`（插件 discover 的 glob 与之一致）；对 2026-09-21 / 09-24 真跑 `discover` 命中 3 个会话，`parse` 出 3267 / 12181 / 25448 字符的可读转写。原告警作废*
  - **2026-09-24 更正：上面那句 ⚠️ 是错的，本机 `~/.kimi-code/sessions` 一直有 11 个真实会话。** 对真实数据探测的结论：`_classify` 对 39206 个事件认出 **0 条**，且 `time` 是 int 毫秒（原实现只接受 ISO 串）→ 归属日永不命中 → `discover` 每一天都返回 0 refs。FR-002 的 kimi 源自建库起就在静默空转，而 7 条单测全绿——它们测的是猜出来的形状。已按实测形态重写解析并换掉夹具（11 条，先红后绿），另加 `test_real_sessions_are_recognised` 当格式漂移闸口（无真实数据的机器 skip）。实测形态记在 research.md 的 kimi 行。
  - **连带更正 struggle 语义**：原来"人一开口即归零"的理由是"wire 里没有 tool 结果事件"，同样被证伪——真实数据有 3669 条 `tool.result`，其中 108 条带 `isError`。现在与 claude_code / codex 一致：只有工具真成功才打断失败连击。两规则在本机真实数据上的差别：达到 struggle_rounds≥3 的会话 4/11（旧）vs 1/11（新）。
- [x] T023 [P][US1] `src/plugins/trae/`：pre-summarized 轻转换 + `trae_type_map` 映射，未匹配默认 reflection（FR-002/013）——*7 个单测（tests/unit/test_trae.py）：每条记录一条素材（discover 按行返回 SourceRef）、text 仅由 learned/outcome 拼装、类型按 map 键序首匹配/未匹配落默认、project 编码不可无歧义还原宁缺毋错；已用本机真实 ~/.trae-cn 数据只读冒烟验证（4 条记录/日，中文内容正确提取）*
- [x] T024 [P][US1] 各插件 fixture 单测 + integration：四源真实数据混跑（AC-002 多源）——*单测见 test_codex/test_kimi_code/test_trae；integration（test_sync_four_sources.py）用内嵌 Qdrant（:memory:，同 upsert/query 代码路径）跑通六源（四插件+git+manual）端到端 sync：6 条 upsert、source 集合齐全、同日重跑幂等；**并抓出 T011 真实 bug：git log 未加 --date=iso-strict 导致 %ad 非 ISO 解析崩溃，已修**；真实服务器验证保留在 test_ingest_qdrant.py（Qdrant 不可达时 skip，make up 后自动启用）*
- [x] T025 [US1] integration：插件目录改名 → sync 成功 + 日志（AC-006）——*test_sync_four_sources.py::test_missing_source_dir_sync_still_succeeds：素材目录指到不存在路径 → sync 成功、claude_source 缺席、日志含 missing no-op、其余五源正常入库；插件包目录层面的导入失败跳过已由 test_registry.py 单测覆盖*

**Checkpoint**: US1 完整——每日沉淀端到端可用，幂等、脱敏、熔断全有测试。

## Phase 4: US2 带过滤的语义检索（M3 → M4 关联边）

**Independent Test**: `make ask Q="…" --type error --since 7d` 带引用回答（AC-004）

- [x] T026 [US2] `src/ask.py`：嵌入问题 → 向量检索 + payload 过滤（type/date/project，FR-018）→ 引用式回答（`[date] type: 摘要`，FR-019）——*9 个单测（tests/unit/test_ask.py）：相对/绝对日期解析（7d/ISO）、Qdrant Filter 构建（type/project/DatetimeRange）、top_k 走配置、引用上下文格式 `[YYYY-MM-DD] type:`、无命中给 make sync 引导且不调 LLM、CLI Q=/--type/--since/--no-expand、失败非零码*
- [x] T029 [US2] `ask.py` 扩展：三模式（off/all/阈值，`retrieval.expand.mode`）+ "关联补充"上下文标记 + `--no-expand`（FR-020/021）——*6 个单测（test_ask_expand.py）：all 模式去重+关联补充标记、off 空扩展、数值阈值按与问题的 cosine 过滤、neighbor_limit_per_hit/context_cap 约束、--no-expand、上下文含（关联补充）；取数用 qdrant retrieve by id（similarity.py 保持 ★ 未动，扩展失败降级不拖垮主回答）；**2026-09-25 整块搬到 `src/ask_expand.py`** —— 流式改动把 ask.py 顶到 315 SLOC 撞了 size 门禁（≤300），把引用构建（`citations_from_hits` / `Citation`）+ 一跳扩展（`expand_neighbors` / `_cosine` / `_citation_from_record` / `_client`）搬出去，ask.py 235 SLOC；`Citation` 定义随之搬家（两边都要用，放上层会成环），ask.py 继续导出保 `from src.ask import Citation` 可用。搬迁时抓到一个**假通过**：`monkeypatch.setattr(ask_expand, "expand_neighbors", boom)` 对 `from ... import` 绑定的 ask.py 无效，而 `test_query_survives_expansion_failure` 的断言在替换失效时照样成立，已改为打在 ask 上并加 `len(calls) == 1` 钉住*
- [x] T027 ★[US2] `src/similarity.py` 关联边构建：**用户授权 AI 实现（2026-09-24）**——top-5 中 >0.75 建边、单条上限 5、对方已满跳过回填、确定性（FR-017；AC-005 前置）——*`build_related_edges` 返回 new_related/backfill 两表：搜索 limit=len(batch)+5（排除本批保幂等）、score>0.75 严格、单条上限 5、对方已满跳过 E→N 回填；9 个单测（空输入/长度不符报错/collection 缺失/双向边/阈值严格 >/邻居已满跳过回填/排除本批/回填去重/同批累积）*
- [x] T028 [US2] `ingest.py` 接入关联边构建 + 重跑不产重复边（AC-003 关联面）——*upsert 对保留条目调 build_related_edges：新条目 related 与既有 related 合并去重封顶 5，邻居经 set_payload 回填；4 个单测（建边+回填/无邻居不建边/重跑去重/既有边合并）；neighbor 用真实 UUID 串*
- [x] T030 [US2] integration：过滤生效（AC-004）、扩展开关与标记（AC-005）；ask 单次耗时实测记录（plan Technical Context，G2 补）——*test_ask_filters.py（内嵌 Qdrant + fake embed/chat）：type+date 过滤仅命中窗口内 error、DatetimeRange 对 "YYYY-MM-DD" payload 实证有效、默认扩展带（关联补充）标记、--no-expand 后消失；**ask 实测（2026-09-24）：CLI 103.9s / API 46s；限长后（2026-09-25）：71.6s；关 thinking 后（2026-09-25）：CLI 5.91s —— 达标原目标 ≤10s***
  - 归因（2026-09-25 分阶段实测）：embed 0.3s + search/expand 0.2s + **chat 71.1s**，生成占 99%；且 `max_tokens` 并非瓶颈 —— 单独测 max_tokens=30 仍要 38s、=200 要 43.5s。
  - **归因修正（2026-09-25，重要）**：先前把 71.6s 记成"doubao-seed-2.1-lite 的 TTFT 天生 ~20–40s"是**误判**。真实原因是 doubao 系模型**默认跑思维链**（响应里带 `reasoning_content`），首字延迟全花在思考上。请求体加 `extra_body={"thinking": {"type": "disabled"}}` 后 TTFT 33.4s → 1.6–4.1s，整轮 **5.91s**。据此：① `retrieval.disable_thinking`（默认 true）走配置开关，`ark_client.chat/chat_stream` 新增 `thinking` 形参逐调用通道；② **目标收回 ≤10s**（2026-09-25 用户确认）—— 那次放宽到 ≤60s 建立在错误归因上，发现 thinking 后撤回；③ 蒸馏**保持 thinking ON**（关掉虽快 1.4×，但单批产出从 9 条涨到 28 条，条目质量/数量变化未经评估，不拿它换时间）。
  - 流式（2026-09-25 新增，用户批准"CLI + SSE 端点"）：`ark_client.chat_stream` + `ask.query_stream`（与 `query` 共用 `_prepare`，保证两条路径的过滤/扩展/引用格式不走样）+ CLI `--stream` + `GET /ask/stream`（SSE：citations → 逐块 chunk → done，生成中途失败补 error 事件）。实测首字 **0.7s**（检索 0.2s 后 0.5s 开始出字）、600 块 / 1061 字符。**真 HTTP 增量性已用 uvicorn + curl 逐行时间戳证实**（citations @T+0 → 首 chunk @T+0.56 → done @T+8.6）；此前 TestClient 里"602 个事件全在 8.13s 一起到"是 TestClient 的缓冲假象，不是缺陷。
- [x] T031 [US2] 性能检查：典型日 sync ≤5 分钟实测记录（NFR-006 / SC-011）——*三次真跑（Qdrant 本地容器、Ark plan 通道）：**2026-09-18 = 188.06s**（8 条素材 / 234,021 字符 / 2 次 chat 往返 / 27 条入库）；**2026-09-23 = 291s**（26 条素材 / 1,332,282 字符 / 12 条入库）。均 ≤300s，但 09-23 那天已用到预算的 ~97%，素材再翻一倍就不是"慢一点"而是违约；**2026-09-24 = 983s（16.4 min，45 素材/30 入库）—— 超 300s 预算 3.3 倍，预警兑现，根因同为 LLM 生成耗时***
  - **并行化修复（2026-09-25，用户批准方案）**：`distill.parallel_workers` 配置项 + `ThreadPoolExecutor.map` 并发跑各批（`pool.map` 保序 = 合并结果与串行一致，`_cap_entries` 取前 N 条不会裁错）。同一天（09-24，57 素材 / 13 批）三次对照：**串行 983s → 4 workers 338s → 8 workers 215.9s**，**达标 300s 预算（用掉 72%）**。workers=4 时 13 批要跑 4 轮、末轮 1 批空转 3 个 worker，是 338s 收不进去的直接原因。
  - 实测过程中炸出来一个真缺陷并已修：单次 chat 的**输出**被服务端 completion 上限掐断（实测 `finish_reason='length'`、6894 字符 / 6865 tokens 处断），`DistillError: LLM returned invalid JSON`，而 `_parse_or_retry` 的重试是原样再发一遍 → 同一处再断一次，整天 0 条入库。修法是按 `distill.batch_max_chars`（新配置项，默认 120000）把一天切批蒸馏，见 `src/distill_batches.py`；决定成败的是输出长度不是输入长度（输入大 5.7 倍的 09-23 反而一次就过）。
- [ ] T020 [US1] **Dogfood ①**（M5，按 plan 顺序置于检索/关联边之后）：用本项目开发会话跑通全链路，记录首印象（LG-001）——*2026-09-24 真跑：sync 983s（45 素材→30 条目→30 入库，Qdrant 39→69 点）；ask CLI 103.9s / API 46s；四端点 curl 全过；服务器版集成 7 过。**LG-001 首印象待用户手写**。实测发现：① 关联边 0/69 点（同日互不连 + 跨日 cosine 未过 0.75），ask expanded=0；② ask 不带 --project 混入他项目素材；③ NO_PROXY `*.volces.com` 不生效，须 `volces.com`，SOCKS 需 socksio*
  - **2026-09-25 复核：观察 ① 已过期**。T027/T028（`similarity.build_related_edges` + `ingest` 接线）落地后重跑，当前库里 **126 个点里 16 条有 related 边**；真跑 `make ask` 输出里也出现了 `error（关联补充）:` 条目（AC-005 的真实数据面）。观察 ② 不是缺陷而是设计（不带 `--project` = 全库检索），是否加默认项目收窄属产品决定，未改。

## Phase 5: US3 手动快记（M8 入口，随 T019 可先行）

- [x] T032 [P][US3] `make log m="…"`：追加时间戳行到 `notes/inbox.md`（FR-004）；未标类型默认 reflection、轻处理不改类型（§8 v0.4）——*src/log.py：`- [ISO时间戳 #type] 文本` 行格式与 collect.NOTE_LINE 对齐，t= 可选标注；CLI 与 API 壳共用 log()*
- [x] T033 [P][US3] 单测：快记入库走轻路径、不进 LLM（FR-013）——*6 个用例（tests/unit/test_log.py）：时间戳+类型标注、无标注无 marker、行可被 collect 解析且默认 reflection、log→collect→distill 全链 chat 零调用、CLI m=/t= 解析*

## Phase 6: US4 范围选择（M8）

- [x] T034 [US4] `make scope`：交互式 工具×项目 矩阵（最近会话日期、估算条数）→ 写 `config/scope.json`；纯文本配置是唯一事实来源（FR-005）——*src/scope.py + 7 个单测：矩阵估算仅用插件契约（discover 近 7 天窗口，不触插件内部）、repos.txt 项目行、t/a/n/s/q 命令、保存 JSON*
- [x] T035 [US4] `collect.py` 尊重 scope.json 过滤。验证：取消勾选后 sync 跳过该工具（AC-007）——*sync 透传 scope.json（坏 JSON 降级不过滤）+ gather 过滤工具与项目；单测：取消勾选的插件 discover 零调用、其余正常入库；取消勾选的仓库不跑 git log*

## Phase 7: US5 校准与运维（M7）

- [x] T036 [US5] 重蒸馏对照：指定日重跑 → diff（共享 source_refs 且相似度 ≥0.85 配对=改写；仅新=新增；仅旧=消失，FR-023）→ 确认后整组替换（先删后插）——*src/redistill.py + make redistill D= [APPLY=1]；7 个单测：快照加载/缺失报错、diff 编排（对齐器注入）、先删后插（date filter delete）、无 --apply 只打印 diff、diff 与替换共用同一批新条目（蒸馏非确定不重跑）；**对齐规则本体是 ★ T037 接缝（similarity.align_redistill），未就位时明确指引***
- [x] T037 ★[US5] 对齐规则实现在 `src/similarity.py`（T027 同文件，用户授权 AI 实现 2026-09-24）+ diff 单测——*`align_redistill`：共享 source_refs（交集非空）且 difflib.SequenceMatcher 相似度 ≥0.85 配对，文本相同=unchanged、有差异=rewritten，未配对新/旧=added/removed；贪心按相似度降序配对保确定性；用 difflib 而非向量余弦是因核心层不能 import ark_client.embed；9 个单测覆盖空输入/改写/未变/新增消失/无共享 ref/阈值含 0.85/贪心选最高/交集非子集*
- [x] T038 [P][US5] 历史日期补跑验证：`make sync D=<过去日>`（AC-009）——*test_sync_four_sources 以固定过去日 2026-09-18 跑通六源全链（行为与当日一致：快照/蒸馏/入库/幂等）；真实数据补跑待 .env + make up 后记录*
- [x] T039 [US5] retention：`raw_retention_days=0` 时清理过期 raw、库内条目不动（AC-008）——*unit 三态（90/0/null）+ integration test_retention.py：过期快照清理、今日快照保留、库内 2 条目数不变；retention=0 边界=清理所有早于今日的快照*

## Phase 8: 入口完备 + 收尾（M8–M9）

- [x] T040 [US1] `src/api/app.py` FastAPI 薄壳：`/health`、`POST /log`、`POST /sync`(后台)+`/sync/status`、`GET /ask`；路由仅参数校验（FR-024；宪法 II）——*后台线程跑 sync.run（互斥靠 data/.sync.lock，运行中 409）；/health 报 Qdrant+四环境变量状态；/ask 失败转 502 附引导*
- [x] T041 [US1] API 冒烟：四端点 curl 通过（AC-001 后半）——*7 个 TestClient 用例（tests/unit/test_api.py）：health 组件状态、/log 空文本 422+核心调用、/sync 202+状态轮询+409 互斥、/ask 引用返回+缺 q 422+失败 502；真实 curl 冒烟（2026-09-24）：/health /log /sync/status /ask 四端点全 200（POST /sync 未触发以免 16 min 真同步）*
- [x] T042 [P] AGENTS.md（命令、目录速查、指向 constitution/PRD 不复制）+ README（含隐私数据流向说明，PRD §9）（M9）——*两文件已建；README 含源目录只读/写入边界/脱敏/蒸馏数据流向（与方舟使用一致）/备份迁移四段隐私说明*
- [x] T043 [P][US6] 用 `_template` 写一个测试插件验证自动注册（US-6 / AC-011 插件面）——*tests/unit/test_plugin_extension.py：按契约在临时父包内建 plugdemo → iter_plugins 自动发现 → gather 采集到素材，核心零改动*
- [x] T044 [US1] 配置新增类型零迁移验证：schema.json 加类型 → sync 识别（AC-011）——*同一测试文件：新增类型 insight 在 LLM 路径与直并入路径均被接受，无代码迁移*
- [x] T045 [US1] 全量 AC 回归（14 条逐条过）+ **Dogfood ②**：ask 本项目开发史（LG-001；M9）——*AC 回归结论（2026-09-20，169 单测+集成全绿）：AC-002/003/004/006/007/008/009/011/012/013/014 ✅ 测试实证；AC-001/005/010 部分 ✅（/health 冒烟过、扩展机制过、重蒸馏编排过——dashboard 真访问/真实边数据/对齐规则待环境或 T027/T037）；**2026-09-25 补实证：三条的"待"均已落地 —— AC-001：Qdrant dashboard HTTP 200、`make embed-test` 真调 2048 维通过、`/health` 返回 `{"status":"ok","qdrant":true,"env":{全 true}}`；AC-005：当前库 126 点中 16 条有真实 `related` 边（T027/T028 落地后重跑所得，非 fixture），真跑 `make ask` 输出含 `（关联补充）` 条目；AC-010：T037 `align_redistill` 对齐规则已实现（用户 2026-09-24 逐任务授权 AI 实现），9 个单测覆盖空输入/改写/未变/新增消失/无共享 ref/阈值含 0.85/贪心选最高/交集非子集。**；**2026-09-24 补记：sync 983s / ask 46–104s 实测超预算（NFR-006 与 ≤10s 违约，根因 LLM 生成耗时）；服务器版集成 7 过 + curl 四端点 200；Dogfood ① 跑通但 LG-001 首印象待用户；embed-test 真调仍待。**2026-09-25 更新：两项均达标 —— sync 并行化后 215.9s ≤300s（NFR-006）；ask 关掉默认思维链后 5.91s ≤10s（目标已收回，见 T030）；另新增 `--stream` / `GET /ask/stream` 流式输出，首字 0.7s***

## Phase 9: v0.7 采集面扩展（M10）

**前置**: Phase 1–8 全部完成（v0.1 已交付）。本 phase 的四步**顺序不可换**——理由见 plan.md "v0.7 采集面扩展"节。
**Independent Test**: 每个源都能在自己的真实数据上产出素材，且源目录零写入（AC-015/016/017）。

- [x] T046 [US6] 迁移脚本 `scripts/migrate_trae_source.py`：按 `source="trae"` 捞出旧点 → 以新 source `trae_work_cn` 重算 point_id → upsert 新点 → 删旧点；默认 dry-run，幂等（AC-016 / PRD §8）——*两个超出原计划的必要补充：① **快照也要改标签**——`data/raw/*.json` 的 `materials[].source` 不迁的话，日后 `make redistill D=2026-09-18` 会把旧 source 的条目写回库里，迁移被无声撤销（实测 2026-09-18 有 5 条、2026-09-23 有 4 条）；② `--restore <备份> --apply` 还原模式——一次性、不可逆的库改动没有回退路径等于裸奔。备份落在 `data/migrations/`（含向量与快照原文）。`make migrate-trae`*
- [x] T047 [US6] 迁移**独立校验**：直接查 Qdrant 现况（**不读脚本自己的中间产物**）断言条目总数不变、正文哈希集合逐一对应；`--apply` 重复跑无副作用（AC-016）——*17 个单测（`tests/unit/test_migrate_trae_source.py`）+ 实跑：126 点 → 126 点，9 条 `trae` → 9 条 `trae_work_cn`，并**逐点重算** `ids.point_id(payload.source, payload.date, payload.text)` 与实测 ID 比对（不一致 0 条）；复跑输出"已经迁过了，无操作"。**过程中真拦下一次自伤**：首版 `plan_points` 只换 ID 不换 payload 里的 source，upsert 后 9 条成了"ID 换了、标签没换"——条数、指纹、总数三条校验全绿，是"按新来源查得到吗"那条红的。校验拦住了，`--restore` 还原，补了 `stale_points()` 与两条单测再重跑。缺的那条测试就是漏掉的那步*
- [x] T048 [US6] `src/plugins/trae/` → `trae_work_cn/` 改名（`MEMORY_DIR` 不变，仍 `~/.trae-cn`），三处 `source="trae"` 改为 `"trae_work_cn"`。**必须在 T046/T047 之后**——先改名会让同一条素材以两个 ID 并存——*`git mv` 保留历史；模块内 3 处 source + `PLUGIN.name` + 4 处日志前缀；4 个测试文件里的引用同步（`test_trae.py` → `test_trae_work_cn.py`）。`kind="trae_record"` **不改**——那是素材类型不是来源名，两个 trae 插件共用。586 测试全绿，layers/size/boundary 三门禁过*
- [x] T049 [US6] `scripts/write_boundary_check.py` 增规则：`src/` 中任何 `sqlite3.connect()` 未用 `file:…?mode=ro` URI 即判红（NFR-001 / AC-017）——*新增第三类硬法 `sqlite-mode`（登记救不了，只能改代码）。判据不是"路径落在产品根下"（那三条现有规则够不着它：没有 mode 参数、不落在 `WRITE_METHODS` 里、路径也不是 `Path.home()` 派生的字面量），而是**打开方式**。三个条件缺一不可，且三种漏法各漏各的：`mode=ro` 缺失（`file:` 的默认 mode 是 **rw**）、`uri=True` 缺失（那个字符串被当成普通文件名，sqlite3 会在 cwd 凭空建一个 `file:…?mode=ro` 的库）、路径读不出字面量（看不懂就按最危险的算，同 `_mode_of` 的原则）。导入形状认三种（`import sqlite3` / 带别名 / `from sqlite3 import connect`）——漏一种就等于换个写法整条溜过去。7 个新单测，含两条对照组：写对了必须放行（否则门禁会成为绕开它的原因），以及**别的库的 `.connect()` 不许误伤**（误报会直接把门禁送进 SKIP，那时候真正该拦的也没了）。顺带修了文件头与 `--list` 里那份早已过期的来源清单（还写着 4 个源）。**独立验证**：把 T050 的夹具单独喂给门禁，输出恰好一条、规则名恰为 `sqlite-mode` —— 证明红来自这条规则而不是邻近的"未登记"*
- [x] T050 [US6] `scripts/gate_selftest.py` 注册 T049 的植入违规用例，确认它真的报红——不会红的门禁比没有门禁更糟（AC-017）——*新增 `write-boundary / 「SQLite 源没按只读打开」` 用例；`make gate-selftest` 33 个用例全部符合预期（该红的 13 条全红、干净对照组 15 条全绿）。为什么这条必须在 B 族之前落地：门禁自己没被证明会红的话，B 族三个插件就是在无人看守下写 SQLite —— 而它们恰好是第一批会碰源目录的写入语义的操作*
- [x] T051 [US6] A 族：`qoder` + `qoder_cn` 插件（逐字段同 schema，但**各自实现**解析器——不得互相 import，见契约规则 6；**时间戳双编码**须先判类型）+ 单测（含"ISO 与 epoch 毫秒混排都解得出"）
  - **`qoder` 已完成**（2026-09-25）。证据：`src/plugins/qoder/__init__.py` + `tests/unit/test_qoder.py`（27 条）。实现前先对本机真实数据（3 会话 / 38629 行）做了一轮只读勘察，**勘察推翻了最初的设计前提**：时间戳并不是"消息类也可能混毫秒"，而是**按记录类型严格二分**——`user`(7397) / `assistant`(13524) 一律 ISO 字符串，`active-leaf`(14412) / `runtime-config`(240) 一律 epoch 毫秒，另有 1016 条无时间戳的 bookkeeping。双编码仍然必须支持（`active-leaf` 占文件一多半且排在用户发言之前，只认字符串会 `AttributeError` 崩掉整个源），但**理由从"混排"改成了"bookkeeping 占比过大"**。注入物实测也修正了：user 角色 7397 条里真人发言只有 146 条，user 记录 258 个 text 块里 146 个是人的——按"user 角色就取 text"会多收 112 条机器文本（压缩摘要 106 + 任务通知 5 等），**虚增 77%**；故用 `origin.kind == "human"` 正标记（允许列表）而非黑名单。助手侧同取 text 块（tool_use 7051 / thinking 5071 / redacted_thinking 431 全丢）。FR-008 一并落实：失败判据是 `toolUseResult.isError` 或 `exitCode != 0`（实测 138 条），正文取 `content[].content`；**与 codex 不同、与 kimi_code 一致**：失败标志是显式的，正文为空也照样记账（codex 要从正文里*找*信号，所以它没正文就不算）。
    - **变异测试实证**（8 个变异体，逐个注回实现后跑本文件）：取消 human 正标记 → 4 条失败；假定时间戳一律字符串 → 7 条失败（`AttributeError: 'int' object has no attribute 'replace'`）；thinking 计入正文 → 1 条；工具成功不结束连击 → 1 条；`discover` 只问"当天有没有记录"（不要求产素材）→ 2 条；失败无正文时不记账 → 1 条；换掉 `"\n---\n"` 连接符 → 1 条；归属日按 UTC 而非上海 → 21 条。**全部被杀**。其中"假定时间戳一律字符串"最初没被杀到——我先写的探针（删掉毫秒分支但保留类型判断）其实语义等价于返回 None，是无害的，换成真正贴近该 bug 的写法才有 7 条红；这条记录在此是为了说明"变异体选错等于没测"。
    - 全量回归：620 passed；`lint_layers` 28 文件、`size_guard` 28 文件/191 函数、`write_boundary_check` exit 0、`gate_selftest` 33/33；源目录跑完 `discover`+`parse` 后 176 个文件的哈希逐一不变（宪法 V 实证）。
    - **新增的隔离义务**：`tests/integration/test_sync_four_sources.py` 的 fixture 原本只把 4 个插件指向 tmp，新插件一落地就去读开发机真实家目录（qoder 2026-09-18 那天真有素材，`upserted` 从 6 变 7）。已补 `qoder.PROJECTS_DIR → tmp`，并在 fixture 内留注释：**v0.7 剩下 6 个插件接入时同样要补，隔离面随插件数增长（转 T058）**
  - **`qoder_cn` 已完成**（2026-09-25）。证据：`src/plugins/qoder_cn/__init__.py` + `tests/unit/test_qoder_cn.py`（26 条）。实测绘形：12 会话 / 34513 行。
    - **同一套 schema ≠ 同样的分布，而分布差异直接改变过滤的分量。** 与 `qoder` 逐项对照：`origin.kind="human"` 284（qoder 146）、**`task-notification` 65（qoder 5，13 倍）**、`isCompactSummary` 10（106）、`isMeta` 25（88）、`redacted_thinking` **0**（431）、human 记录里的 image 块 **5**（0）、`toolUseResult` 为**字符串** 2（0）、失败工具调用 77（138）。结论：照抄一份"只丢 `isCompactSummary`"的实现，在 qoder 上看着没事，在 qoder_cn 上多收 65 条机器文本。过滤规则相同，**分量完全不同**，所以两个插件各自用自己的实测计数写 fixture。
    - 时间戳双编码同样成立（`user` 5968 / `assistant` 12670 为 ISO；`active-leaf` 12505 / `runtime-config` 450 为 epoch 毫秒；1466 条无时间戳）。另外两条 qoder 没有的形状各自补了单测：`toolUseResult` 是字符串时**没有失败证据**，既不该崩也不该平白记一次失败；human 记录里的 image 块（base64）被 `type == "text"` 正过滤挡掉。
    - **`paths allow-list 的深度是安全边界**：`projects/*/*.jsonl` 恰好命中 12 个会话文件，把 113 个 `subagents/*.jsonl`（子代理转写，与 codex 丢 `agent_message` 同理）和每个会话旁的 `state.json` / `compression-v2/state.json` 一并挡在外面。两条单测分别钉住这两类文件不被读入（FR-002a 的 NFR-001 路径允许列表）。
    - **变异测试**（11 个变异体，跑 `test_qoder_cn.py` + 同源等价性测试）：丢 human 正标记 → 6 条失败；假定时间戳恒为字符串 → 9 条；把 thinking 当正文收 → 2 条；改用黑名单"非 isMeta 即用户输入" → 2 条；工具成功不重置连击 → 1 条；失败无正文不记账 → 1 条；不设转写上限 → 1 条；`discover` 不看日期 → 3 条；任何 dict 形式 `toolUseResult` 都算失败 → 3 条；`discover` 丢掉"当天真能产素材"判据 → 2 条；`parse` 不筛归属日 → 2 条。**全部被杀**。
    - 两个"存活"变异体的处置（这是本项最有价值的两条记录）：① **"不做块类型过滤"最初没被杀**——因为 thinking 块的键是 `thinking` 不是 `text`，那个变异体语义等价于原实现，是**空探针**；换成贴近真实 bug 的写法（`block.get("text") or block.get("thinking")`）后 2 条红。与 qoder 那次同病：**变异体选错等于没测**。② **"给失败加回正文为空的守卫"存活**是真的测试缺口——qoder 那边我加过 `test_parse_counts_a_failure_that_produced_no_output`，写 qoder_cn 时漏了，已补。同一轮还补了 `test_parse_drops_an_unknown_origin_kind`（构造一个 `isMeta`/`isCompactSummary` 都不设的**未知**注入类型），因为原来的 task-notification fixture 同时带 `isMeta: True`，把黑名单式实现给**掩护**过去了——而"新注入类型默认被挡住"正是选允许列表的全部理由。
    - 全量回归：**649 passed**；`lint_layers` 29 文件、`size_guard` 29 文件/203 函数、`write_boundary_check` exit 0、`gate_selftest` 33/33。
    - **只读实证（宪法 V）**：对真实 `~/.qoder-cn` 连续跑 3 轮 16 天 `discover`+`parse`（每轮 28 素材，3.07s），6122 个文件中唯一变化的 `logs/runs/*/qodercli.log` **不是插件写的**——Qoder CN 应用本身在跑（进程里可见 `daemon-server`），且连续三轮同一份日志哈希不变，而 `projects/*/*.jsonl` 的 glob 结构上到不了 `logs/`。**这条值得记下来是因为第一轮脚本报了"1 个文件变了"，差点被当成违规放过——追下去才发现是应用自己在写。**
  - **FR-002a "两者 schema 逐字段相同"这句话经核对成立，不需要改 PRD**：它说的是**字段结构**（两边都是 `type` / `origin.kind` / `toolUseResult.exitCode` / `message.content[].type`），实测两边字段名确实一致；上面那些差异是**取值分布**的差异，不是 schema 的差异。v0.7.1 已经规定"各自实现"，所以也不存在"据此推断一个解析器能服务两边"的风险。记在这里是为了避免下次勘察时把同一件事再提一遍。
  - **T057 的计时数据点**（非结论）：qoder_cn 单源 16 天 discover+parse 全程 3.07s，远低于 300s 预算——但这不是 NFR-006 意义上的"典型日单源计入 sync 全程"，T057 仍待做。
- [x] T052 [P][US6] A 族：`workbuddy_ai` 插件（CodeBuddy 系**独立解析器**；正文只取 `<user_query>…</user_query>`）+ 单测。**断言判据是标签本身，不是计数比例**——v0.7.1 已把 v0.7 的"89 条 user 记录只产 32 条素材"更新为当日真值 116/47，且该数字随使用增长，写死计数会把一条会过期的数字钉进测试
  - **已完成（2026-09-25）**：`src/plugins/workbuddy_ai/__init__.py` + `tests/unit/test_workbuddy_ai.py`（29 条）。断言全部落在**标签与尾注本身**（"无标签的 user 记录不产素材"、"尾注为 0 不算失败"），没有一条把 116/47/26 这类会随使用增长的计数写死。
  - **实测复核了 FR-002a 的两处措辞，都成立**：① 时间戳**单编码**——5326 条记录无一例外是 epoch 毫秒整数（qoder 系是 ISO 字符串与整数在同一文件内混排），"连时间戳策略都相反"属实；② "时间戳统一 epoch 毫秒"属实。首条样本字数也复核一致：整块 12392 / 标签外 12354 / 标签内 13 / 标签本身 25 → 13+25=38，与 PRD 引用的"38 字是本人输入"**逐字对上**。
  - **注入物是唯一难点，实测分量**：`message` 678 条 = user 116 / assistant 562；116 条里**只有 47 条**带 `<user_query>`，另外 69 条是机器物（`task-notification` 53、`conversation_history_summary` 6、"Please continue…" 6、`system-reminder` 2、`teammate-message` 2，后两者 `content` 是**字符串**而非块列表）。"role=user 就取正文"多收 69 条；真正的大头是标签外的正文——全库最大一条 user 记录整块 **19486**、标签外 **19429**、标签内 **32**。故 user 侧的正标记取**标签本身**（比块类型名强，且不怕块类型改名），assistant 侧才用块类型允许列表（实测 562 条块全是 `output_text`，且没有一条记录带多于一个块）。另实测 user 侧 1 条 `image_blob_ref` 块（只有 `blob_id`/`mime`/`size`/`blob_path`/`original_filename`，**无 `text` 键**），按 text 取正文天然跳过。
  - **失败信号在尾部包裹层**（`Command:` / `Stdout:` / `Stderr:` / `Exit Code: N` / `Signal:`）：1552 条结果里 783 条带尾注（757 为 0；26 条非零 = `137`×12 的 SIGTERM + `1`×14，全来自 Bash）。三个假阳性陷阱都实测过并写进模块 docstring：① 小写 "exit code" 命中 789 次，绝大多数是**被回显的命令原文**；② `is_error` 命中 11 次，全是 pytest 覆盖率报告里我们自己代码的函数名（`src/plugins/claude_code/__init__.py:_is_error`）；③ `FAILED` 命中 24 次，与"退出码非零"**零重叠**（抓到的全是"跑通过的测试提到了 FAILED 字符串"），是比退出码严格更差的信号。另 60 条 Bash 结果是后台启动、47 条是块列表输出（read_me / show_widget / present_files），**都没有尾注**——没有证据就不算失败。
  - **尾注取"最后一条"而不是第一条**，并有一条测试专门钉住：命令自己的输出完全可能印出以 `Exit Code: 1` 开头的行（`cat docs/format.md`——那份文档讲的正是这个格式），取第一个匹配会平白记一次失败。
  - **不伪造字段**：实测源里**没有** `gitBranch` 键（qoder 系才有），故 `meta` 只有 `cwd` 与 `session_id`。一条恒为 `None` 的 `git_branch` 等于声称"该源有这个字段但它是空的"，而事实是这个源没有它；测试直接断言 `"git_branch" not in meta`。另：此前已 grep 确认**下游没有任何地方读 `git_branch`**，所以这个字段在 qoder 系那边也只是记着。
  - **变异表 14 条，10 杀 4 存活**（全部已分诊，**没有一条是真缺口**）：M1 丢标签过滤——11 杀；M2 取首个 `Exit Code`、M3 无尾注也算失败、M4 时间戳加 ISO 兜底、M5 成功结果不归零连击、M6 `first_ts` 取最后一条、M7 加回 `git_branch`、M8 路径深度放宽到 `**/*.jsonl`、M10 `struggle = error_count`、M13 失败不算"当天有素材"——各 1 杀。四条存活逐条查了**真实数据**才定性：**M9**（去掉 assistant 的 `output_text` 允许列表）——实测 562/562 块都是 `output_text` 且无记录带多个块，没有可达路径，是对未来块类型的防御；**M11**（去掉 `bool` 守卫）——`True` 会解成 1970，日期过滤本来就会跳过它，等价变异；**M12**（标签内容不 `strip`）——实测 47 条真实标签内容 strip 前后**零差异**（探针 `/tmp/wb_probe9.py`），等价变异；**M14**（`Exit Code` 正则去掉行首行尾锚）——实测 769 条无尾注的结果里**零条**含 `Exit Code:` 字样，且 783 条有尾注的记录里锚定与松散匹配列表**逐条相同**，等价变异。这四条不为它们编测试：为没见过的形状写测试，测的是我的想象。
  - **变异驱动补了一条真缺口**：M13 暴露 discover 原本没有任何测试钉住"当天只有工具失败、没有 user_query"的会话——实测 26 条失败里 12 条是 SIGTERM（被中断的长/后台命令很常见），那样的会话当天可能一条标签都没有，漏掉它快照里连失败证据都不剩。已补 `test_discover_finds_a_session_whose_only_material_is_a_failure`。
  - **真实数据实跑**（单测用的是自造 fixture，只有真跑才能验证"注入物没漏进来"）：四天 discover+parse，`error_count` 合计 **26**（与实测一致）、结构性注入标记（`<user_info>`/`system-reminder`/`task-notification`/`<user_query>` 等 10 个）泄漏 **0 处**、47 条真实标签正文**逐条原样**出现在素材里、源文件哈希变动 **0/5**。
  - **实跑探针自己错了两次，都不是插件的错，记下来免得下次重犯**：① 拿 `SOUL.md` 当注入标记 → 误报，因为助手会**正当地**在正文里说"我动了你的 SOUL.md"（与"回显命令里的 exit code"同类：看着像证据的子串不是证据）；② 拿"当天全部会话的标签正文"去比对"单个会话产出的素材"，粒度不一致，探针自报"缺失 74 条"而真值只有 47 条——**74 > 47 本身就证明判据自相矛盾**。按 (文件, 日) 分组后归零。
  - **一个良性碰撞，记录备查**：素材分段用 `"\n---\n"` join，而模型爱写 markdown 分隔线 `---`（实测 6 条 assistant 正文含该串，故探针按分隔符切段会多切）。已确认**下游没有任何地方按这个分隔符切分**（只有插件用它做 join），且这是所有源插件共有的既有性质，不新增风险，不改。
  - **门禁**：678 passed（+29）；`make layers` 30 文件；`make size` 30 文件 / 212 函数；`make boundary` exit 0；`make gate-selftest` 33/33。`tests/integration/test_sync_four_sources.py` 已补 `workbuddy_ai.SESSIONS_DIR → tmp`——**但要说清楚：这条 patch 目前不是承重的**，DAY=2026-09-18 而实测会话跨度是 09-20…09-23，指漏了也不会改变结果；它防的是将来那天出现数据（T058 一并复核）。
- [x] T053 [US6] A 族：`trae` 插件（`~/.trae`，与 `trae_work_cn` 同格式同 `trae_type_map`，**各自实现**）+ 单测——*先侦察后动手：`~/.trae` 只有 3 个会话文件 / 5 条记录（日期 2026-09-20 三条、09-21 两条），密度远低于 `~/.trae-cn` 的 55 文件 / 706 条。用 `/tmp/trae_cmp.py` 在两个根上对撞，**逐条核对 PRD FR-002a 的"同格式、同映射规则，仅 MEMORY_DIR 不同"**：记录键集合完全一致、`learned`/`actions` 恒为 list、`outcome`/`intent` 恒为 str、`message_summary_time` 706/706 与 5/5 都能按 `%Y-%m-%d %H:%M:%S` 解出、没有一条正文为空的记录、同级兄弟文件都只有 `topics.md`（`~/.trae` 的项目目录级另有一个 `project_memory.md`）。差异只在数据量与日期集合——**PRD 的断言在真实数据上成立**，不是照抄 `trae_work_cn` 的假设*
  - **先写测试**：`tests/unit/test_trae.py` 14 条，先见其 RED（`ImportError: cannot import name 'trae' from 'src.plugins'`），再写插件转绿。比 `trae_work_cn` 的 7 条多出的 7 条不是凑数，是补上那一份**缺失的边界覆盖**：同级兄弟文件、时间戳兜底落零点、discover 与 parse 之间文件被删、schema 坏掉仍要能并入、`project` 留空而非猜测解码、未匹配字段落默认类型、注册表登记。
  - **`project` 一律 `None`**：项目目录名把路径分隔符编码成 `-`，项目名本身含连字符时无法无歧义还原；实测真实目录名如 `-Users-nava-Code-llm-rag-first-rag--p2-a36559cd5ee996ba2f18`（`--p2-` 后还挂哈希），甚至 `brary-Application-Support-TRAE-SOLO-…-vkzdoq--p2-e22ad8c9`（**开头就是残缺路径，没有盘符前缀**）。与其给下游一个会误导过滤的值，不如留空——payload 允许 project 为 null。
  - **一处相对 `trae_work_cn` 的有意差异**：不复制它 `parse` 里那句 `path = ref.ref.partition("#L")[0]`——赋值后从未使用，是死代码（见 T053 收尾说明）。
  - **门禁**：695 passed（+17）；`make layers` 31 文件；`make size` 31 文件 / 218 函数；`make boundary` exit 0；`make gate-selftest` 33/33。

- [x] T053a [US6] **同源插件对等价性测试**：`qoder`/`qoder_cn` 与 `trae`/`trae_work_cn` 各喂一份同样的 fixture，断言除 `source` 外 `RawMaterial` 逐字段相同（含 `ts`、`kind`、`meta`）。这条是"各自实现"这个选择的**唯一代价补偿**——没有它，两份实现漂移时没有任何东西会响（PRD FR-002a / 契约规则 6）——*两组各 3 条，共 6 条，`tests/unit/test_same_source_plugin_pairs.py`。*
  - **trae 那一组刻意走真实数据走不到的分支**：实测两个根的记录清一色 `%Y-%m-%d %H:%M:%S`，所以 ISO 兜底、时间戳读不出落零点、条目级空白过滤这三条在生产路径上**永不触发**——正因为不触发，漂移了也没人发现，才更需要这条测试盯着。fixture 按真实形状写文件，但**以原始行列表写入**（第 1 行留空、第 2 行坏 JSON），这样 `#L<n>` 的行号忠实性本身也在被比——一边按"有效记录序号"编号、另一边按"文件行号"编号就会错位。
  - **不是空对空**：先断言 fixture 确实产出了可观内容（正文含 learned/outcome、不含 intent/actions/`compact_summary_meta` 的机器字段、`note_type` 与 `ts` 逐条对得上），再比等价。恒真的断言比没有断言更坏。
  - **变异表（这次是补做的，不是事后追认）**：trae 半边首次运行即全绿——**首次即绿的测试在见到它红之前什么也没证明**，所以回过头拿 9 个"两份独立实现最可能漂成什么样"的变异体压它：**8 杀 1 存**。被杀：引用编号改按有效记录序号（M1）、丢掉 ISO 兜底（M2）、列表字段不过滤空白（M3）、正文拼装顺序颠倒（M4）、正文漏掉 outcome（M5）、类型映射取最后一个匹配而非键序第一个（M6）、时间戳兜底落 UTC 零点（M7）、`project` 改成猜测解码（M8）。qoder 半边另压 3 个，**2 杀 1 存**（去掉 `origin.kind=="human"` 正标记、时间戳不转上海时区）。
  - **两个存活者都判为空洞变异，且都做了实测才敢这么判**：① M9「discover 不再跳过空白行」——任何纯空白字符串都过不了 `json.loads`（实测 `''`/`'   '`/`'\t'`/`' \t \n '` 全部 JSONDecodeError），那句 `if not line.strip()` 只是快路径，不是可观测行为；② Q3「epoch-ms 分支 +1 天」——先用注入 `raise` 证明该行**确实可达**（所以"不可达"这个省事的解释是错的），再查它为什么还是没人抓：`_file_hits_day` 与 `parse` 都要求「时间戳落在当天」**且**「该记录有素材」两者同时成立，而 epoch-ms 时间戳只出现在 `active-leaf` 这类 bookkeeping 记录上，它们不产素材，取值随即被丢。真实数据也量了：**两个根里带素材的记录，时间戳 1255/1255 与 2165/2165 全是字符串**，无一条走 epoch-ms 分支。故**不为此编造测试**——空洞变异该如实记下，不该拿一个注定恒真的用例去凑"全部被杀"。
  - **顺带修了自己写坏的一处断言**：落 trae 半边时编辑 `new_string` 手滑，把 qoder 那半边的 `(a.ts, a.kind, …)` 打成了 `(a.ts, b.kind, …)`——既让测试变红，又悄悄丢掉了 `a.ts`/`b.ts` 的比对（两边 `kind` 同为 `"message"`，退化成一个恒真的自我比较）。是测试变红暴露的；已还原并复跑。
  - 同一份 fixture **写两份、字节相同**再分别喂给两个解析器（连"内容是否真相同"都不必赌），fixture 把每个分支都走一遍：人类发言、助手 text、thinking、成功/失败工具结果（`isError` 与 `exitCode` 两种判据）、压缩摘要、任务通知、未知来源注入物、毫秒与无时间戳 bookkeeping、别天的记录、字符串形式的 `toolUseResult`。
  - **两条防线**：① `parse` 逐字段比对（除 `source`）；② `discover` 比对发现口径——后者更要紧，它决定某天到底有没有这条源。
  - **不做空对空**：如果两份实现都返回空素材，逐字段比较会"通过"而什么都没证明。所以先钉住 fixture 确实产出了可观的文本与 `struggle_rounds`/`error_count`，再比等价。
  - **证明它会红**（否则等于没有）：注回"丢掉 human 正标记"这个真实 drift 后，`parse` 那条给出逐字 diff（任务通知、压缩摘要、字符串 payload 记录全部漏进来——正是那个 77% 虚增 bug 的缩影）。
  - **一处被探针暴露的弱项已补**：注入式 drift 下 `discover` 那条**不会红**——只要文件里有人说的话，两边都会说"这天有素材"，哪怕一边把注入物也算成了正文。故补 `test_qoder_pair_agrees_on_an_injected_only_session`（整条会话只有注入物，两边都该认为不构成素材），它才是真正盯着 `_file_hits_day` 里 `_material_text(record)` 判据的那条。补完后同一次 drift 有 2 条红。
- [x] T054 [P][US6] B 族：`opencode` 插件（`session_message`；**表允许列表**排除 `credential`/`account`/`control_account`；`mode=ro`）+ 单测——*先侦察后动手（`/tmp/b_recon.py`、`/tmp/b_schema.py`、`/tmp/b_days.py`、`/tmp/b_tools.py`，全程 `mode=ro`）：库内 85 行 `session_message` 全落 2026-09-23；`type` 列与 `data` 键集合一一对应，实测六种组合（user 14 / assistant 48 / assistant+error 4 / assistant−rawFinish 3 / idle 14 / model-switched 2）；assistant 的 `content` 块 reasoning 51 / tool 68 / text 30，只有 `text` 进正文；`tool.state.status` completed 63 / **error 5**，`state.error` 是 `{type, message}` **字典***
  - **失败判据定案**：取 `content` 里 `tool` 块的 `state.status == "error"`；**不取** `idle.outcome == "failed"`——那是会话空闲结束状态，实测 14 条 idle 里有 failed，算进来会把"会话正常结束"误报成挣扎。
  - **`meta.cwd` 恒为 None 是故意的**：真 cwd 只在 `session_v2.directory`，允许列表放宽到它属于**扩权**，需 PRD 变更控制，故留一个诚实的 None，而不是猜一个值。**✅ 已获裁定（2026-09-26）：保持现状**——不放宽到 `session_v2`，最小允许列表优先；`meta.cwd` 恒为 None 计入已知代价而非缺陷。**不改 PRD。**
  - 19 条单测（先见 `ImportError` 红）。**变异表 11 条 → 10 杀 1 存**；存活者「末尾 `sorted(seen)` 换成 `list(seen)`」经查是**空洞变异**：SQL 已 `ORDER BY session_id, seq`，首次见到 session 的顺序必然就是字典序，构造不出行为不同的输入——**不编造测试**。
  - 修掉一处实现错：`session_message.type` 是**列**不是 `data` JSON 的键（写成了 `record.get("type")`，12 条测试同时红）。
  - 真实库跑通：2026-09-23 → 2 会话（21699 字 / struggle 5；816 字 / 0）；2026-09-22 → 0。
- [x] T055 [P][US6] B 族：`zcode` 插件（`message` + `part`；排除 `message.data.synthetic` 为真的记录；`mode=ro`）+ 单测——*`message` + `part` 两表 JOIN，`part.data.type` 实测 text 1003 / tool 1560 / reasoning 515 / step-start 1405 / step-finish 1398 / timeline 13；`state.error` 在这里是**字符串**（opencode 那边是字典——两族各自实现的意义正在这里）。`meta.cwd` 从 `message.data.path.cwd` 白拿，**不必**读第三张表*
  - **PRD 的注入物数字在真实库上逐字对上**：user 343 / `synthetic=True` **115** / 真用户 **228**。并且**插件口径下也恰好是 228**——过滤确实落在插件里，不是只在侦察脚本里成立。
  - 真实库跑通：2026-09-18 → 1 会话 1181 字；2026-09-11 → 1 会话 78667 字 / struggle 2 / error 21 / cwd 正确。
  - **原文的「含 343/115/228 计数断言」与 workbuddy v0.7.1 撤回计数断言的先例冲突**，按仓库先例实现：**断言的是判据本身**（fixture 复现 synthetic 的三分支），会过期的真实计数记在 docstring 与本文件，不钉进测试。
  - 21 条单测。**变异表 12 条 → 12 杀**（首轮 10/12，两个存活者都判为**真缺口**并补测：跨午夜边界、`sequence` 列排序；后者靠 `seq`/`part_seqs` 显式指定与 rowid 脱钩才可观测）。
  - 一处**我自己写错的测试**：`discover` 的期望顺序写成 `[SESSION_A, SESSION_B]`，而实现返回 `sorted(seen)`，`claude-import-0c7f…` 本就排在 `claude-import-1316…` 前。改测试并加注释。
- [x] T056 [P][US6] B 族：`hermes` 插件（epoch **秒**，按毫秒解会得到 year 58684；`mode=ro`）+ 单测。**仅构造 fixture，测试须显式标注"未经真实数据验证"**——该库当前零行数据（FR-002a / AC-017）——***已复核：`messages` 0 行 / `sessions` 0 行，`schema_version` 30。实现依据不是猜的，是 hermes-agent 0.21.5 的源码**（`~/.hermes/hermes-agent/`）：表形状取自 `hermes_state_messages._INSERT_MESSAGE_SQL`；**时间戳是 epoch 秒**（`_coerce_timestamp` 兜底 `time.time()`，不是毫秒）；`content` 是 TEXT，多模态内容带哨兵前缀 `"\x00json:"`（`hermes_state._CONTENT_JSON_PREFIX`）；**失败形状是 `tools/registry.tool_error()` 写的 `{"error": …}`**，工具异常经 `model_tools._sanitize_tool_error` 包成 `"[TOOL_ERROR] …"` 再塞进该字段（`:644` 构造、`:964` 落库）*
  - **注入物过滤：FR-002a 对本源没有给规则**（zcode 有 `synthetic`、workbuddy_ai 有 `<user_query>`，hermes 只有"零数据、按 schema 实现"）。因此**不施加任何注入物判据**，只按 role 与空值过滤——挑哪条属于需求决定，须回 PRD 变更控制。库里确有候选列（`_compressed_summary` 是产品自己的压缩摘要、`active`/`compacted` 标注被取代的行、`observed`、`display_kind`），**是否排除留待补验时定**。
  - **已知缺口（写进 docstring）**：不复制 `agent/display._detect_tool_failure` 的整张判据表（terminal 的 `exit_code`、`success is False`、degraded、guardrail refusal）——零数据下无法验证哪几条真的会落库。
  - 允许列表 `{messages, sessions}`；`system_prompts`（系统提示词全文）/`state_meta`/`gateway_routing`/`session_model_usage`（计费）一律不读，单测植入哨兵钉住。凭据文件 `~/.hermes/.env` 与 `auth.json` 在库外（PRD §NFR-001 点名）。
  - 22 条单测，**含一条机械钉住 AC-017③ 免责声明的用例**（断言测试与实现里都留着"未经真实数据验证"字样）。**变异表 11 条 → 11 杀**（首轮 9/11，两个存活者都判为真缺口并补测：跨午夜边界、多模态哨兵）。
- **⚠️ 三源共同发现（`mode=ro` 不保证不落 `-wal`/`-shm`）→ ✅ 已获裁定（2026-09-26）：只订正措辞 + 记风险，行为不变**：契约第 30 行 / NFR-001 写的理由是"普通 `connect()` 会在源目录落 `-wal`/`-shm`，那就是写入"，暗示 `mode=ro` 不会。**实测这个暗示是错的**，对照实验（`/tmp/roshm`，干净 WAL 库只有 `t.db`）：`sqlite3.connect("file:…?mode=ro", uri=True)` 打开后目录变成 `['t.db', 't.db-shm', 't.db-wal']`——只读连接**自己造出了**这两个文件。另两个边界：① 源目录**不可写**时 `mode=ro` 直接打不开（`attempt to write a readonly database`，此时靠 `_query` 的 `except sqlite3.Error` 降级为空数据）；② `immutable=1` 能开且零落盘，但**会忽略 `-wal`**（opencode 的 `-wal` 有 5.6 MB，最新会话会静默丢失），**不能选**。
  - 这意味着 **AC-017① 的成立是有条件的**：它的措辞是"无 `-wal`/`-shm`/**新增**"，而真实源目录里这两个文件本来就存在（产品在运行），所以跑完列表确实不变（实测只有 `-shm` 的 mtime 变，`db.sqlite` 本身未动）——但一旦产品干净退出把 sidecar 删掉，我们的读就会把它们造回来。
  - **另一处测试隔离缺口（同源问题，T057 前发现并修复）**：全量 pytest 会**读到真实源目录**——三处测试在测别的目标时顺带遍历真插件（`test_scope.py:69` `build_matrix()`、`test_scope.py:114` `collect.gather()` 只 patch 了 `_repo_commits`、`test_collect.py:65` `collect.gather()`），各补一行 `iter_plugins` patch（同文件已有该惯用法）。**留痕实验**：修前三处都跑时 zcode/hermes 的 `-shm` mtime 被改写、修后纹丝不动；套件 10.67s → 1.63s。仍在 NFR-001 已知边界内（`db` 文件与 `-wal` 始终未被碰、opencode 全程未动），但测试不该碰用户真实数据目录——与 T054–T056 期间被迫补的集成 fixture 是同一类问题，故一并修掉。
  - **裁定结果与落地**（PRD v0.7.2，2026-09-26）：① `PRD.md` NFR-001 增「已知边界」条目（含源目录不可写、`immutable=1` 不得采用两条推论），§9 风险表增 v0.7.2 行；② AC-017① 补明「该条在产品运行期间成立」，sidecar 被产品删除后重建属已知边界、不计违规但 T058 须记录实测前置状态；③ 同步修订 `spec.md` NFR-001 与 SC-019、`contracts/plugin-contract.md` 规则 1；④ 三插件 `_connect()` docstring 从"普通 `connect()` 会落 `-wal`/`-shm`"这个**暗示 `mode=ro` 不会**的表述改为显式的边界陈述。**代码零变更**——B 族自始即按 `mode=ro` 实现，本次是把真实边界写进需求，不是改行为。**没有为了措辞去动 SQLite 连接参数**：`immutable=1` 能零落盘但会静默丢 `-wal` 里的最新数据（opencode 的 `-wal` 5.6 MB），用数据陈旧换目录干净是错的取舍。
- [x] T057 [US1] **⚠️ 任务设计已修正：逐源计时测不出 NFR-006**（2026-09-26 实测发现）——原措辞是"每源单独 `make sync D=<该源有数据的日期>` 记耗时"，但真实 sync 把**当天所有源的素材合并后统一分批**（`split_for_batches` 跑在合并后的 materials 上），成本**不是按源可加的**。真正的驱动量是「当天全源字符总量 → 批数 → 轮数」。故 T057 改为**按天计时**，逐源只作辅助。
  - **逐源实测 3 个**（scoped sync，只留一个源）：`trae_work_cn` @ 09-22 **1.7s**（58 素材 / 6706 字符，`distill 0.0s`——走 pre-summarized direct 路径，零 LLM）；`opencode` @ 09-23 **124.0s**（2 素材 / 22515 字符 → 1 批）；`claude_code` @ 09-25 **196.4s**（44 素材 / 1716305 字符 → 16 批 / 8 workers = 2 轮；collect 0.3s / distill 194.8s / ingest 1.3s）。
  - **成本模型**：**distill 独占 ~99%**。collect 实测 1.2–16.3s（**订正**：早先写的"恒 ≤2s"只成立于热缓存——冷缓存下同一个 09-25 要 16.3s。差异来自我自己的 sha256 全库校验把页缓存挤掉了，不是代码问题）。轮数 = ceil(批数/8)，**实测单轮延迟 97s（claude_code 2 轮 194.8s）与 124s（opencode 1 轮）**——这就是 NFR-006 的真正约束项。免 LLM 的只有 `trae`/`trae_work_cn`（`kind == "trae_record"`，`distill.py:89/:121` 判据），其余 9 源都要真蒸馏。
  - **30 天全源离线扫描**（`collect.save_snapshot` 置空，不落快照）：字符总量最高三天为 **09-25 = 1,928,545（18 批 / 3 轮）、09-24 = 1,892,862（19 批 / 3 轮）、09-23 = 1,519,323（13 批 / 2 轮）**；其余 27 天全在 1 轮内。**`max_raw_chars = 2000000` 的封顶在 30 天内一次都没触发**（最高 96%），所以真正兜底的是批数而非封顶。
  - **投影**：最忙日 3 轮 × 实测单轮 97–124s = **291–375s，骑在 NFR-006 的 300s 上**。即 11 源下预算会**间歇性被突破**，且方向取决于方舟单轮延迟的波动。这不是"逐源累加"的结果，是"最忙那天的全源合并"的结果。
  - **✅ 决定性实测（2026-09-26，最忙日 09-25，不设 scope 的全源 sync）**：`{'materials': 53, 'entries': 30, 'upserted': 24, 'raw_removed': 0}`，**端到端 248.7s / 300s**（83%，余量 51s）。
  - **投影被实测证伪（向好的方向）**：投影 291–375s，实测 248.7s。18 批 / 8 workers = 3 轮 ⇒ 实测 **≈83s/轮**，低于两个单源数据点的 97s 与 124s——批数多时单轮延迟反而**摊薄**（单批那轮更接近长上下文的冷启动代价）。**结论：投影式估算在这条链路上会系统性偏悲观，未来估预算只认实测。**
  - **NFR-006 处置**：**最忙日未突破，预算不动**（用户裁定方向为"先不动预算"，但该方向是"若逼近/突破"的预案；实测未触发，故不进入优化）。**未测的**：另 29 天（含 09-24 的 1.89M / 19 批）与 run-to-run 方差——只有 1 个样本，51s 余量在方舟延迟波动下不保证每次都够，此风险如实记录，不粉饰为"达标".
  - **一条观察（不改，仅记）**：circuit breaker 把 180 条裁到 30 条，即花 248.7s 蒸馏出 180 条只用 30 条。属 FR-010 熔断的既有设计，本次不动，但它是后续压缩耗时最直接的下手处。
- [x] T058 [US6] AC-015/016/017 回归 + 源目录只读实证（任一 SQLite 插件跑完后源目录文件列表与跑前逐一致）——***隔离面已提前补齐（T054–T056 期间被迫做）**：`tests/integration/test_sync_four_sources.py` 的 fixture 现已把 11 个源全部指向 tmp（补 `trae.MEMORY_DIR`、`opencode/zcode/hermes.DB_PATH`）。这不是预防性重构：**zcode 在 2026-09-18 当天有真实会话，漏掉它会让 `upserted` 从 6 悄悄变成 7**，集成测试实测变红后才发现——即 T051 里那条"`workbuddy_ai.SESSIONS_DIR` patch 目前不承重"的判断，在 zcode 上是反的。T058 仍需补的是 AC-017① 的**实证**本身。`mode=ro` 与 `-wal`/`-shm` 那条发现**已处置完毕**（PRD v0.7.2：只订正措辞 + 记风险，见上），但实证时须按 AC-017① 订正后的措辞**额外记录跑前的 sidecar 前置状态**——若产品已干净退出而 sidecar 缺失，`mode=ro` 会把它造回来，这属已知边界、**不计违规但必须写下来**，否则这份实证读起来像是"从不落盘"，正是本次要修掉的那个过度承诺。*
  - **✅ AC-017① 只读实证（2026-09-26，09-25 全源 collect，不调 LLM）**：跑前 sidecar 前置状态已记录——三个库的 `-wal`/`-shm` **跑前就都存在**（opencode `-wal` 5.5MB、zcode/hermes 的 `-wal` 为 0 字节），**不是**"产品干净退出后 sidecar 缺失"那种会触发自造的边界情形。跑后比对：**新增 0 / 消失 0 / 大小变化 0 / sha256 内容变化 0**；唯一变化是 zcode 与 hermes 的 **`-shm` mtime**（即 NFR-001 已知边界本身，非违规）。**`db` 文件逐字节未变**——这是比"文件列表一致"更强的一条。opencode 连 `-shm` 都未动。
  - **凭据边界实证**：`~/.hermes/.env` 与 `auth.json` mtime **未变**（且插件只开 `state.db`，凭据文件从不出现在任何读路径上）。**订正一处我自己的操作**：第一版校验脚本的 glob 把 `auth.json` 也纳入了 sha256 计算（只算哈希、未打印、未落盘），已改为凭据文件只比 mtime、不读内容——校验脚本本身也不该碰凭据。
  - **✅ AC-017②**：`make gate-selftest` 33/33，含"SQLite 源没按只读打开 期望红 实际红"。
  - **⚠️ AC-015 部分通过**（2026-09-26 重核后由 ✅ 降级；原文只记了「素材非空」，而 AC-015 要的是「素材非空**且入库成功**」两半——原因见 PRD §13 v0.7.7 的 spec 漂移）：
    - **v0.7.9 更新（2026-09-26）**：AC-015 正文已改为只声明**观测对象**（PRD §10），方法下沉 `quickstart.md`；判据所需的两个计数已落地（`distill_run.per_source` / `sync` 汇总的 `skipped_by_source`，见 T082）。**状态仍是「部分通过」——变的是「可判」，不是「已验」**：`qoder` / `qoder_cn` / `workbuddy_ai` / `zcode` 的入库路径仍未被真跑过，下一步按 `quickstart.md` 的复制还原法补（不阻塞交付）。
    - **注册插件数 = 11** ✅ 实测。
    - **素材非空** ✅：10 源在各自有数据的日期产出非空素材，`hermes` 30 天内仍零数据（AC 明确豁免）；注入物排除按 FR-002a 逐源单测（zcode 真库 343 user / 115 synthetic / **228** 真用户与 PRD 逐字一致）。
    - **入库成功** ⚠️ **仅 `trae` 有日期级验证**：2026-09-26 单源重跑 `make sync D=2026-09-21`（scope 收窄至 `trae`、关掉 git）——2 素材 → 2 条目 → 2 入库、`model=direct`、退出码 0；快照 `distill_run` 由单键 `{"status":"noop"}` 变为三键 `{"model":"direct","rubric_hash":"d74dfaa…","status":"ok"}`，Qdrant 219 → 221、`source="trae"` 首次非零。
    - **`opencode` 的入库路径历史上走通过**（库中有 9 点），但当时未留日期级验证记录。
    - **`qoder` / `qoder_cn` / `workbuddy_ai` / `zcode` 的「入库成功」未验——且经查是「从未被尝试过」，不是「试了没成」**：全库 source 计数为 `claude_code 156 / trae_work_cn 39 / opencode 9 / kimi_code 8 / codex 7 / trae 2`，这 4 个源**一个点都没有**；它们那几份快照停在采集期标记，而写这些标记的是采集探针（蒸馏根本没被调用）——见 T060 的 v0.7.7 订正。
    - **为什么不补跑（判断，非结论）**：入库段与来源无关——`distill._direct_entries` 只按 `kind` 分流、全文无按 `source` 的分支，LLM 路径亦已被 `claude_code` 等 156 个点在真库上跑通；且补跑原本要走的「单源跑一天」那条路本身会破坏 `data/raw/` 重放基线。是否补验留待用户裁决。
    - **方案对照已备（AI 整理）：`specs/001-learning-memory-rag/ac015-method-options.md`** —— 两个方案的成本/影响逐项对照 + 本轮实测的三条新证据（现存 7 份快照仅 3 份多源；`zcode`/`workbuddy_ai`/`hermes`/`opencode` 在现存任何快照里都没有素材；`qoder` 有素材、当日 `ok` 却 0 点，证明「素材非空 ≠ 入库成功」且二者**不可分辨**）。选型与 AC 正文写法仍待用户裁决。
  - **✅ AC-016 处置（2026-09-26，用户裁定：给脚本加防护、默认拒跑）**：
    - **发现的不成立**：历史迁移**已应用**，但 dry-run 仍报「待改快照：1 个文件、2 条素材（`2026-09-21.json`）」，而其 `ref` 指向 **`/Users/nava/.trae/memory/...`**——**是新 `trae` 插件的合法数据**，不是待迁的旧 `trae`（那批实为 Trae CN，在 `~/.trae-cn/`）。根因：脚本判据**只看 source 名、不校验 ref 路径**，而 v0.7.1 把 `trae` 这名字**让给了新插件**复用。后果：`--apply` 会把新 `trae` 的素材错标成 `trae_work_cn`，且**不报错**。SC-018 的"重复应用无副作用"因此失效——它当初成立只因那时没有别的数据叫 `trae`。
    - **改法（判据换成路径）**：`is_old_trae_ref()` 只认 `~/.trae-cn/memory/` 前缀；`plan_points` 要求 `source_refs` **全部**落在旧目录（混合、空、指向新目录一律排除 —— fail-safe：证明不了就不动）；`relabel_snapshot` 同判据看 `ref`；新增 `foreign_refs()` 汇总"挂旧名、ref 却在新目录"的条目。目录常量从 `trae`/`trae_work_cn` 两个插件取（宪法 I），不另抄。
    - **闸门**：`main` 检测到 `foreign_refs` 非空即**默认拒跑**（退出码 2，打印每一条）；`--force` 才继续，且**仍只迁旧路径的**。理由：这是一次性迁移且早已执行完，还能扫出 foreign 条目 = 有人在回头重跑它。
    - **TDD 留痕**：先 RED（24 failed），实现后 24 passed。**RED 是靠 `OLD_MEMORY_DIR` 缺失触发的 AttributeError——那证明不了防护逻辑被钉住**，故补做变异检验：同时摘掉 `plan_points` 与 `relabel_snapshot` 的路径判据 → 恰好 4 条防护测试红（`..._excludes_...` ×3、`..._leaves_new_plugin_materials_alone`）、其余 20 条绿；还原后 24 绿。
    - **实跑证据（真实数据，只读）**：默认运行 → **exit 2**，抓到 **2 条** foreign ref，全部来自快照 `data/raw/2026-09-21.json`（Qdrant 侧 `source=trae` 点数为 **0**，全库 219 点、`trae_work_cn` 39 点 —— 即 foreign 引用**只在快照里**，与上述根因吻合）。`--force` → 打印"只迁旧目录下的条目"后报「待迁移条目：0 条 / 待改快照：0 个」→ **exit 0**，语义正确。
    - **未做（有意）**：没有改 `2026-09-21.json` 快照，也没有给 `data/migrations/` 那两份备份做任何处理——两条都在用户裁定的范围之外，留给后续决定。
- [x] T059 [P] 文档同步：README 与 AGENTS.md 的源清单（当前只列 4 个）、`~/.trae-cn` 更名说明、SQLite 只读与凭据排除边界。**必须在对应插件真的落地后才写**——提前把 11 个源写进 README 是在描述一个还不存在的系统
  - **落地（2026-09-26，11 个插件全部落地后）**：README 新增「**支持的数据源**」表（source → 产品目录 → 读法，11 行）；首段"四个 AI 编码工具"→ 11 个、架构图左列改为"11 个采集源"；「隐私与数据流向」的"源目录只读"一条从 4 个目录扩到 11 个，并新增两条边界 —— **SQLite `mode=ro`**（含实测到的"不保证不落 `-wal`/`-shm`"与为何不用 `immutable=1`）与**凭据双向排除**（路径 + 各源 `ALLOWED_TABLES`）。AGENTS.md 同步两处：`plugins/` 目录注释列出 11 个并按 JSONL/SQLite 分族；硬约束里的产品源目录由 4 个改为指回 README 的表。
  - **两处刻意写得比结论更弱**：① 凭据排除只实测过 hermes 那一对（`.env`/`auth.json` mtime 未变），其余路径明写"依据实现推断，**未逐条实测**"；② `~/.trae-cn`/`~/.trae` 更名说明里写明迁移脚本"**判据是 ref 路径、重跑默认拒跑**"——不写这句，读者会以为重跑无害（正是 AC-016 那个坑）。
  - **门禁**：`make hooks-run` 15/15 绿；`make baseline` 基线一致（1788 / 189 / 14 → 90.4%）。

**Checkpoint**: registry 注册插件数 = 11；每个新增源在真实数据上产出素材且注入物被排除；源目录零写入有机械门禁兜底。

## Dependencies & Execution Order

- **Phase 1 → 2 → 3 严格串行**（基础设施 → 契约 → 主链路）；T021/T022/T023 三个插件彼此 [P] 可并行
- **Phase 4 依赖 T013**（ingest 先在）；Phase 5/6 依赖 T019（sync 先在）
- ★ 任务（T015/T017/T027/T037）**排期按用户手写速度**，不受 AI 生成速度绑架（PRD §12.3）；每个 ★ 任务前用户先写预期行为（LG-003，记入 notes/）。T004/T012/T015/T017/T027/T037 为已获用户逐任务明确授权的例外，不得扩大到其他核心模块
- 同 phase 内 [P] 任务可并行；跨 phase 禁止（纵切完整性优先）
- **Phase 9 内部严格串行四段**：T046→T047→T048（迁移必须先于改名，见 §8）→ T049→T050（门禁必须先于 B 族，否则 B 族是在无护栏下写 SQLite）→ T051–T053（A 族）→ T054–T056（B 族）→ T057–T059。A 族与 B 族内部各自 [P]
- [x] T060 [US6] **快照写保护 + 恢复被覆盖的 `data/raw/`**（T059 收尾自查发现，PRD v0.7.4）
  - **发现**：收尾核对时发现 7 份快照里有 5 份的 `collected_at` 是同一秒（今天 16:13），且各只含**一个**来源。对照 `data/migrations/trae-rename-20260925T073708.json`（09-25 迁移备份，内含当时的快照原文）：`09-18` 从 **8 条**（claude_code 2 / kimi_code 1 / trae 5，`ok`）变成 **1 条**（zcode，`noop`）；`09-23` 从 **26 条**（claude_code 5 / codex 17 / trae 4，`ok`）变成 **1 条**（workbuddy_ai，`noop`）。根因不是代码缺陷而是**方法缺陷**：AC-015 的验证方式是"每个新源挑一个有真实数据的日期**单独** `make sync D=<date>`"，而 `collect.save_snapshot` 是裸 `write_text`（`src/collect.py:199`）——覆盖不合并，于是每天的单源跑都把当天多来源快照换掉。
  - **影响面（要说准）**：**Qdrant 未受影响**——条目是 upsert，09-18 仍有 27 点、09-23 仍有 21 点。受害的是 `data/raw/` 作为 `make redistill` **重放基线**的性质：此后 `make redistill D=2026-09-18` 会拿"1 条素材的日"去对照"库里 27 点"，**不报错，只给一个错的 diff**。这正是"审计基线"这条性质被打破的样子。
  - **写保护（TDD + 变异检验）**：`save_snapshot(day_raw, *, allow_shrink=False)` —— 同日已存在快照的素材数**多于**新快照即拒写（`SnapshotShrinkError`，写之前抛，文件不动）；已存在但**读不出来**同样拒写（解析不了就无从判断会不会削减，宁可停下）；`allow_shrink=True` 是明路，经 `sync.run(allow_shrink=…)` 与 `make sync ALLOW_SHRINK=1` 接通（**拒导但不可放行的闸门只会逼人去删文件**，而删文件更糟）。`distill` 回写 `distill_run` 时素材数不变，不被拦。
    - RED：6 failed（`SnapshotShrinkError` 不存在 / `gather` 不收该参数）+ 2 passed（无防护时"写入"本就成立）；CLI 接线 RED：4 failed。GREEN 后 **781 passed**。
    - 变异检验：① 摘掉 `and not allow_shrink` 短路 → 2 红；② `>` 改成 `>=` → 3 红（含 `..._allows_growth_and_same_count`，即掩护了 distill 回写那条路）。还原后 39 绿。
  - **恢复**：从 09-25 迁移备份还原 `09-18`（8 条）与 `09-23`（26 条），重标 `trae`→`trae_work_cn`（5 条 / 4 条）**复用被守护的 `migrate.relabel_snapshot`**——判据是 ref 路径，不另写一份。恢复前先把"被覆盖后的现状"留底到 `data/migrations/raw-single-source-<stamp>.json`。复原后实测：两份顶层键为契约四键、`distill=ok`、残留旧 source **0**、`trae_work_cn` 中 ref 不在 `~/.trae-cn` 的 **0**。
    - **有意不恢复的一部分**：被覆盖掉的 1 条 zcode（09-18）与 1 条 workbuddy_ai（09-23）**没有并进去**。理由是"恢复到事故前"语义更干净：这两条是新近的单源验证跑采到的，源目录里还在、随时可再采；而 8/26 条原始素材的**采集时点**已过（`collected_at` 分别是 09-24 / 09-23），不可复得。取舍写在这里，不留暗账。
  - **不可恢复的 5 天（如实记录）**：`09-21`（现 2 条 trae）、`09-22`（58 条 trae_work_cn）、`09-24`（5 条 qoder_cn，而库里该日有 **87** 点 claude_code）、`09-25`（53 条 / 4 来源）、`09-26`（1 条 qoder）**没有备份**。其中 `09-24` 最明确：快照已完全无法解释库里的 87 点。**这些日期的 `make redistill` 对照不可信**，已同步写进 PRD §9 风险表 v0.7.4 行。
  - **残留风险（未根除）**：写保护只看「素材数」，"同数量但换了来源"的覆盖拦不住。要根除得改验证方法本身（先复制 `data/raw/` 再验证，或走 `gather` 的纯函数路径而不落盘）——**本次未做**，因为那属于方法/流程变更而非本次事故的处置，留作后续。**v0.7.8 拆分处置**：本条含两半，**第一半（写保护判据不够）已关闭**，见下条；**第二半（验证方法本身）仍未做**，随 AC-015 的裁决一起走。
  - **v0.7.7 订正方法归因（2026-09-26 重核）**：本任务把事故方法记为「每个新源挑一个有真实数据的日期**单独** `make sync D=<date>`」。该归因**对 09-22 / 09-25 成立**（那两次确是真 sync：09-22 为 58 条单源、`ok`；09-25 为 53 条四源、`ok`），**对 16:13 那五份不成立**：五份的 `collected_at` 严格递增、总跨度 **1.105 秒**（09-26 02.683 / 09-24 03.361 / 09-21 03.582 / 09-23 03.633 / 09-18 03.788），而 `collect.gather` 被 `sync.run` 的持锁段包着（`src/sync.py:123`）——五次 `make sync` 连进程启动都来不及、更过不了锁。**唯一相容的解释：同一个进程里的五次 `gather()` 调用**（不持锁、不蒸馏），即一个**只验采集、不隔离落盘**的探针。因此那五份的 `noop` 应读作「蒸馏**从未被调用**」，**不是**「蒸馏失败」；AC-015 的「入库成功」对这几个源**从未被尝试**。
    - **对上一条「残留风险」的补充（该条写于发现上述事实之前）**：其中「走 `gather` 的纯函数路径而不落盘」**经查不可用**——`gather` 就是落盘的那条路（`src/collect.py:91`；其 docstring 明写 persist 属 FR-006），它没有不落盘的模式。所以「写个探针去验采集」这个动作**必然**写 `data/raw/`。根除需给 `gather` 加一个不落盘的模式（或把落盘上移到 `sync.run`）——**本次未做**，留作后续独立变更（属行为变更，须走变更控制）。
    - **方法学教训**：`collected_at` 是本次唯一能把两者分开的证据——它是 `gather()` 的首行取值、又在持锁段内，所以**同一秒 = 同一进程**，这直接证伪了「多个独立进程各自死在半路」的解释。当时记录只写了"5 份在同一秒"，没往这条推论上走。下次遇到同类歧义，先把时间戳分布当指纹用。
  - **v0.7.8 关闭「写保护判据不够」这一半（2026-09-26，PRD v0.7.8）**：`save_snapshot` 的守卫由**一条**判据扩为**三条并列拒写**——①条数变少（v0.7.4 原有）；②**有来源从盘上消失**（关的就是上面被点名的那条「同数量换来源」）；③**快照结构读不出来**（`materials` 缺失/非列表、某条素材给不出字符串 `source`）。
    - **③ 是实施中由变异测试逼出来的，属本版新写代码自身的缺陷**：`_existing_snapshot` 对"JSON 解析不了"拒写，对"解析得出但结构不对"却 `return 0, set()` 读成"盘上 0 条"进而**放行**——而②的判据正要从这里读 sources，于是洞开在守卫以为自己正防着的一侧。是变异体 `return 0, set() → return 1, set()` 和 `isinstance(...) and ... → or` **存活**把它暴露出来的：那两条存活等于说"这条路径一行测试都没有"。
    - **TDD 留证**：5 条新用例先红后绿，其中 2 条刻意构造成 **2 → 2**（`{claude_code,trae}` → `{claude_code,opencode}`），使**条数判据在该处必然哑**——这样它们变绿就只可能是在测来源/结构判据，不可能被条数判据顺手带过。`make test` 788 → **800 passed**。
    - **变异检验（这是本条的判据）**：全量重建后逐条核对，`save_snapshot` + `_existing_snapshot` 的 58 条存活里逻辑类 13 条，**13 条全部落在本版未改动的行上**（`mkdir(exist_ok=)` ×3、`encoding="utf-8"` ×3、`json.dumps` 三个参数 ×6、`isinstance(doc, dict)` ×1）。**新增解析逻辑的逻辑类存活为 0。**
    - **代价如实记录**：两条新错误信息的解释性散文带来约 24 条新存活，变异分数 89.6% → **88.6%**。同一轮里逻辑覆盖是**上升**的（分母 2052 → 2107，被杀 +16、存活 +25）。本仓库里"把错误信息说清楚"与"分数好看"在这一处直接冲突，本版选择前者；该族在 README 分诊表里已按既有裁语标为**已知且接受**，不是待修缺陷。
    - **v0.7.9 裁决（2026-09-26）**：AC-015 的验证方法已定（AC 只声明观测对象、方法下沉 `quickstart.md`），`gather` 不落盘这一半**未做**、已登记为 **T083**（见 PRD §9 待澄清项第 2 条与 `ac015-method-options.md` §4）。
  - PRD v0.7.4 已记：版本头、§9 风险行、§13 变更记录三处。spec 的 SC-018 不涉本条（迁移脚本与快照覆盖是两回事）。

## Notes

- 每任务完成即更新本文件勾选状态；测试先写并确认失败再实现（TDD 仅适用于非 ★ 任务，★ 任务用户自定节奏）
- 提交时机由用户决定（宪法 Development Workflow）

## Phase 10: Convergence

**来源**：`$speckit-converge`（2026-09-26），对照 spec / plan / tasks / 宪法 与当前代码，共 **18 条 finding**——**CRITICAL 0、HIGH 2、MEDIUM 8、LOW 8**；按类型 missing 0 / partial 13 / contradicts 4 / unrequested 1。归并为 **17 条任务 T061–T077**（T073 把 plan.md 上两条同源漂移合成一条；已按 HIGH → MEDIUM → LOW 排序）。**其中 T066/T067/T077 是只有用户本人能完成的三条**，已在条目里标注【用户】。
**第二次独立复查（2026-09-26，只读子代理，刻意不复用上面的结论）**追加 4 条 finding，合计 **22 条 finding / 21 条任务（T061–T081）**：CRITICAL 0、HIGH 2、**MEDIUM 11、LOW 9**；missing 0 / partial 15 / **contradicts 6** / unrequested 1。该复查同时也报错了两条，**已实测证伪、不予采纳**：`plan.md:96` 与 `:118` 并非"四端点漏 `/ask/stream`"——`:96` 明写 `sync+status`（把 `/sync` 与 `/sync/status` 算一处），`:118` 是对 M8 当时状态的历史陈述。它报的 `quickstart.md:26` 则确属漏网，已并入 T062（同一条命令的第四处）。
**无违宪项**：宪法 I–VII 逐条对照正文核过；写入边界实测无越界（5 个登记写入点未增、`.env`/`data/`/`notes/` 均未被 git 跟踪、无 `scope.json` 残留）；门禁全绿（781 passed / layers 34 / size 34·256 / boundary exit 0 / gate-selftest 33-33）。
**共同病因**：v0.7.1–v0.7.4 的四次变更每次都改了 PRD，但**没有一次回灌到 spec 正文与文档**，于是 spec 里同时留着自相矛盾的 MUST（T061）与被取代的数字（T063）。

**完成情况（2026-09-26 收口）**：21 条中 **18 条已完成并验证**（T061–T065、T068–T076、T078–T081），证据见各条注记末尾的「本轮已落实」；余 **3 条为【用户】自留**（T066 LG-001 首印象、T067 ADR 结论、T077 LG-003 预测留痕；v0.7.9 又追加 **T082 已完成**（AC-015 判据落地）/ **T083 已完成**（`gather` 不落盘 + 只读探针，v0.7.11））—— v0.7.10 又追加 **T084**（同一次运行内部的近似冗余），**已于 2026-09-26 裁定为「不是问题、不改」**（证据见 `fr007-rerun-options.md` §12），按 `Converged with Notes` 处置。收口时门禁全绿（具体用例数 / 规模 / 变异分数以 `make test` / `make size` / `make baseline` 的实时输出为准 —— 这里原先抄着三组数字，当天就漂了三次，故不再抄）。

- [x] T061 [US6] 订正 `spec.md:155` 与同节父项 `:154` 的自相矛盾：qoder/qoder_cn 由「MUST 共用解析器」改为「各自实现 + 等价性测试」 per spec.md:154-155（FR-002a，contradicts）——*`spec.md:154` 写「MUST **NOT** 假定可共用解析器」，隔一行 `:155` 写「MUST 共用解析器」，两条并列且相反。实现是两个独立解析器（`src/plugins/qoder/`、`qoder_cn/` 各约 250 行）；`spec.md:11` 自己记着 v0.7.1 裁决为「各自实现」、`plan.md:144` 写「不要因为长得像就共用解析器」。属 v0.7.1 改了父项与同步头、漏改这一行。**本轮已落实**：已改 `spec.md:155` 为「各自实现 + 等价性测试」，与父项 `:154` 一致；`tests/unit/test_same_source_plugin_pairs.py` 钉住两插件等价。*
- [x] T062 [US2] 修掉 `make ask` 的失效示例：`README.md:60,61`、`AGENTS.md:33,34`、`Makefile:33`、`quickstart.md:26` **四处**把 `Q=… --type error --since 7d` 与 `--stream` 改为经 `ARGS=` 传 per README.md:60（FR-018，contradicts）——***实测 `make ask Q="x" --type error` 退出码 2***：配方只转发 `$(ARGS)`（`Makefile:35`），`--type` 被 make 当未知选项。文档教的正是这个跑不通的写法，而 `tests/unit/test_makefile_forwarding.py:28` 已把唯一入口钉为 `("Q","ARGS")`。`AGENTS.md:17-18` 的 `uv run python -m src.ask …` 写法是对的——即同一文件里两种写法自相矛盾。**本轮已落实**：四处全部改经 `ARGS=`：`README.md:60,61`、`AGENTS.md:33,34`、`Makefile:33`、`quickstart.md:26`。实测 `make -n ask Q=x --type error` → `unrecognized option '--type'`；`ARGS="--type error --since 7d"` 正确转发。
- [x] T063 [US6] 回灌 spec 的两处过期内容：`spec.md:156` 的 workbuddy_ai 计数 89/32 → 116/47（含「判据是标签而非比例」），`spec.md:11` 同步头 PRD v0.7.1 → v0.7.4 per spec.md:11,156（FR-002a，partial）——*PRD v0.7.1 变更记录已把数字换成 116/47（`PRD.md:100` 现文），spec 未同步；同步头停在 v0.7.1 而 PRD 已是 v0.7.4（v0.7.2 的 SC-019/NFR-001 订正其实**已经**写进 spec 正文了，只有头部没跟上）。**本轮已落实**：`spec.md:11` 同步头已到 PRD **v0.7.5**；`spec.md:156` 数字改为 **116/47**，并写明判据是 `<user_query>` 标签本身而非比例。*
- [x] T064 [US6] 把「默认拒跑、退出码 2」回写 `PRD.md` AC-016 per spec.md:217（AC-016，contradicts）——*AC-016 只写「再次 `--apply` 无任何副作用（幂等）」，而 SC-018 已补成「由**默认拒跑 + `--force` 只迁旧路径**保证，故重跑会以退出码 2 停下而非静默重写」。行为无风险（拒跑确实无副作用），但按宪法 I「验收标准的任何变更 MUST 先落 PRD」，PRD v0.7.4 记的却是「无 FR/NFR/AC 变更」——验收的**可观测行为**变了，就该在 PRD 上留痕。**本轮已落实**：`PRD.md` AC-016 已补「由**默认拒跑 + `--force` 只迁旧路径**保证，故重跑会以**退出码 2** 停下而非静默重写；迁移判据是 **ref 路径**」。*
- [x] T065 [US1] 裁决 NFR-006 / SC-011 的验收口径：定为**按天计时**（回写 `PRD.md:157` 与 `spec.md:210`，走变更控制），或补测剩余 8 源 per PRD.md:157 / spec.md:210（SC-011，partial）——*两处仍写「逐源计时，累计逼近 300s 即触发处置」，而 T057 已证明该口径**测不出总预算**（`split_for_batches` 跑在当天全源合并后的 materials 上，成本不按源可加），PRD §9 已订正为按天。且实测只覆盖 **3/11** 源（trae_work_cn 1.7s / opencode 124.0s / claude_code 196.4s）。需求正文与自己的风险表说法不一致，二选一收口。**本轮已落实**：裁决为**按天计时**并已回写：`PRD.md:157`、`spec.md:210`（SC-011）均改为按天，实测 **248.7s / 300s（83%）**。*
- [ ] T066 [US1] 【用户】手写 LG-001「Dogfood 首印象」，完成后勾选 T020 per tasks.md:66（LG-001，partial）——*61 条任务里**唯一未勾选**的一条。两次 Dogfood 都已真跑（① 2026-09-24 sync 983s / ask 103.9s / 四端点 curl 全过；② T045 的 AC 全量回归），缺的只是人写的那一段。这是流程里唯一只有用户能完成的阻塞项。*
    - **2026-09-26 候选措辞已落盘（AI 依用户授权起草）**：`notes/t066-dogfood-material.md` 末尾（占位注释下方）写好一份候选稿，主线取 §3 第 1 vs 第 2 条的反差 —— 「拒答是诚实的」对「缺料时用相邻话题顶替且不声明」。**你把它改成自己的话即算完成**，写完顺手勾本条与 T020。
- [ ] T067 [US6] 【用户】为实施期新增的技术决策补 ADR（结论由用户撰写，AI 只提供素材与选项） per PRD.md:289（LG-005，partial）——*PRD §7 的 ADR 表仍是 ADR-1…11（全部产生于实施前），而实施期至少新增了 6 个够格的决定：蒸馏并行化（`parallel_workers`）、按输出切批（`batch_max_chars`）、思维链默认关（`retrieval.disable_thinking`）、SQLite `mode=ro`、快照写保护（`ALLOW_SHRINK`）、迁移判据用 ref 路径。它们目前只在 tasks.md/§9/§13 里带**AI 写的**理由；仓库无 `docs/adr/`、无 `CONTEXT.md`。*
    - **素材已备（AI 整理）：`specs/001-learning-memory-rag/adr-candidates.md`** —— 6 条候选逐条给「现状（含文件:行）/ 备选（含被否的）/ 当时的证据 / 可逆性 / 待你判断的开放问题」，另附 4 条边界候选（计时按天、键名不改名、变异分数让路文案、README 基线无门禁）与**格式问题**（现有 ADR 表是三列一行式，装不下备选与证据 —— 格式须先定）。结论仍由用户撰写。
    - **2026-09-26 已推进（按建议执行，格式已定）**：格式走 `docs/adr/NNNN-title.md`、编号**从 ADR-12 续**
      （PRD §7 的 ADR-1…11 不动，保持一条序列）。`ADR-c2 / c3 / c5` 三条已落成
      `docs/adr/0012-batch-by-output.md`、`0013-thinking-off-for-ask.md`、`0014-snapshot-write-protection.md`：
      **背景 / 备选与被否的理由 / 当时的证据 / 可逆性与已知代价都已填满，每条末尾三个具体待答问题**，
      只有 `## 决策（待你写）` 一节等你写（状态栏是 Proposed）；`docs/adr/README.md` 是目录说明与写法。
      PRD §7 表下加了一行指针指向它们（**结论未定，所以不上表**）。`ADR-c1 / c4 / c6`
      （`parallel_workers` / SQLite `mode=ro` / 迁移判据用 ref 路径）信息量本来是一行 —— 结论定了回 §7 各加一行，
      不单独建文件。**剩下的只有你写那三段结论**（写完后勾本条并在 PRD §13 加一行）。
    - **2026-09-26 候选措辞已落盘（AI 依用户授权起草）**：三份 ADR 各加了 `### 候选稿（AI 起草，待你改定）` 子节，逐问给出倾向 —— ADR-12：认 120000 但如实记为经验值、跨批断链写成已知副作用并留观测；ADR-13：质量对照不豁免（排在后面）、默认值不改但写明「只关 ask」这个边界才是正确性来源；ADR-14：选「拒写 + 明路」、认下变异分数的代价、并写明「完备性由变异测试守、不由覆盖率守」。**候选稿不算结论**（LG-005）—— 状态仍是 Proposed，你改定后转 Accepted。
- [x] T068 [US5] 重跑 `make mutation` 并 `make baseline-update`，让 README 的质量基线覆盖 T060 之后的代码 per README.md 变异基线（T059，partial）——*`make baseline` 报 **45 个** src/tests 文件 mtime 晚于上次跑批（含 T060 新增的 `save_snapshot` 与 8 条测试）。当前 README 的 `1788 / 189 / 14 → 90.4%` 只与 `mutants/` 这份**旧缓存**自洽，未覆盖最新改动。工具自己提示「mtime 只说明动过，内容可能没变」——要确认就得真跑。**本轮已落实**：`make mutation` 重跑 → **1821 / 214 / 14 → 89.5%**，`make baseline-update` 写回 README，`make baseline` 复核 **✓ 一致**。*
- [x] T069 [US1] `data-model.md:44-52` 的 config schema 块补上 4 个缺失键 per data-model.md:48（FR-007/019，partial）——*文中自称「结构即契约」，但 `distill` 只列 7 键（实际 9，缺 `batch_max_chars`、`parallel_workers`）、`retrieval` 只列 `top_k`+`expand`（实际 4，缺 `answer_max_tokens`、`disable_thinking`）。这 4 个恰好全是实施期新增的。**本轮已落实**：`data-model.md` schema 块补 `batch_max_chars` / `parallel_workers` / `answer_max_tokens` / `disable_thinking` 四键，并注明属**实施期新增**、交叉引用 T067。*
- [x] T070 [US1] 删掉 `quickstart.md:22` 里代码中并不存在的「写回 `.env`」 per quickstart.md:22（FR-002，partial）——*`scripts/embed_test.py` 只做 `config.load_env()`（第 23 行，只读）与建/查集合，**从不写 `.env`**。模型 ID 是人工填进 `.env` 的，文档写成了脚本行为，会让人跑完等一个不会发生的事。**本轮已落实**：`quickstart.md:22` 删掉「写回 .env」，改为「脚本只读 .env，模型 ID 由你手工填」。*
- [x] T071 [P] `README.md:64` 的按需 API 端点清单补 `/sync/status` per README.md:64（FR-024，partial）——*实际 6 个路由（`src/api/app.py:70/82/107/116/122/175`），README 列了 5 个；`AGENTS.md:38` 列了 6 个，只有 README 漏。**本轮已落实**：`README.md:64` 端点清单补齐 `/sync/status`，与实际 6 个路由一致。*
- [x] T072 [P] 订正 `README.md:114` 的 gate-selftest 计数 per README.md:114（AC-017②，partial）——*README 写「27 个用例：14 个该红 + 13 个该绿」；实测 **33 个：18 该红 + 15 该绿**（脚本自己的收尾行也印「33 个用例全部符合预期」）。T049/T050 加 `sqlite-mode` 用例时把这个数字落下了。**本轮已落实**：`README.md:114` 改为「**33 个用例**：18 个"该红" + 15 个"该绿"」，与脚本收尾行一致。*
- [x] T073 [P] 同步 `plan.md` 的代码树与接口摘要 per plan.md:70,132,167,172（plan 决策，partial）——*三处：① `plan.md:70` 的代码树漏掉 8 个实际存在的模块（`sanitize`/`secret_patterns`/`log`/`scope`/`redistill`/`distill_batches`/`distill_candidates`/`distill_messages`）；② `plan.md:167` 写 `chat(messages, json_mode=True)`，实际是 `chat(messages, json_mode=False, max_tokens=None, thinking=True)`（`src/ark_client.py:76`）；③ `plan.md:172` 写 `ask.query(q, filters, expand_mode)`，实际是 `query(question, *, type, project, since, until, expand)`（`src/ask.py:157`）。**本轮已落实**：`plan.md` 代码树补齐 8 个漏掉的模块；`chat(...)` 与 `ask.query(...)` 签名按实现订正。*
- [x] T074 [P] 订正 `plan.md:132` 关于 `trae_type_map` 的错误断言 per plan.md:132（plan 决策，contradicts）——*原文写「`src/plugins/trae/__init__.py:113` 是硬编码字面量读它」，两处都不对：`trae/__init__.py:113` 是空行，而实际读法是 `config.load_schema().get("trae_type_map", {})`（动态、带坏配置兜底）。这条断言会误导后来者去改一个不存在的问题。**本轮已落实**：`plan.md:132` 的假断言已删，改为「读取走 `config.load_schema().get("trae_type_map", {})`，见 `src/plugins/trae/__init__.py:140`——**不是硬编码字面量**」。*
- [x] T075 [P] 订正 `data-model.md:8` 的 content_hash 量纲 per data-model.md:8（FR-014，partial）——*写「text 的 sha256 前 **16 字节**十六进制」，实现是 `hexdigest()[:16]`（`src/ids.py:26`）= **16 个十六进制字符 = 8 字节**。差一倍，且这是条目 ID 的构成部分，量纲写错会让人重算不出同一个 ID。**本轮已落实**：`data-model.md:8` 改为「sha256 十六进制摘要的**前 16 个字符**（即 8 字节；`src/ids.py:26` 的 `hexdigest()[:16]`）」。*
- [x] T076 [US6] 删掉 `src/plugins/trae_work_cn/__init__.py:109` 的死代码 per tasks.md:136（T053，unrequested）——*`path = ref.ref.partition("#L")[0]` 赋值后从未使用；T053 写新 `trae` 插件时已经有意不复制这一行，`trae/__init__.py` 里没有它。留着会让两个同源插件出现无意义的差异。**本轮已落实**：死代码已删（`trae_work_cn/__init__.py` 里 `path = ref.ref.partition("#L")[0]` 赋值后从未使用），两个同源插件的无意义差异消除。*
- [ ] T077 [US1] 【用户】补 LG-003 的预测留痕：为 T027（关联边）补一段对照复盘，或显式豁免 per PRD.md:287（LG-003，partial）——*`notes/` 只有 `t015-prediction.md` 与 `t017-prediction.md`；同为 ★ 的 T027/T037 没有预测记录，而 tasks.md 的 Dependencies 节写着「每个 ★ 任务前用户先写预期行为（LG-003，记入 notes/）」。**预测窗口已过**，强行补写预测等于编造——所以选项是复盘（T027 的关联边现在可以用真实库对照：126 点里 16 条有边）或明确豁免这一条。*
    - **2026-09-26 候选措辞已落盘（AI 依用户授权起草）**：`notes/t027-review.md` 末尾写好一份候选稿，取「显式豁免预测对照 + 事后解读」路线（理由：事后回忆冒充当时的预测，比没有更糟），并把 §3 的两条读数归因为**设计**、把「0.85 对齐线从未被真实触发」归因为**语料**。**你把它改成自己的话即算完成。**

**—— 以下 4 条来自 2026-09-26 的第二次独立复查（见本节开头）——**

- [x] T078 [US1] `PRD.md` §3.1 In Scope 的采集清单补 `hermes` per PRD.md:66（FR-002a，contradicts）——*该节整节 **0 处** `hermes`（`awk '/^### 3\.1/,/^### 3\.2/' PRD.md | grep -c hermes` → 0）；那一行列了 10 个源（claude-code、codex、kimi-code、opencode、zcode、qoder、qoder_cn、workbuddy_ai、trae_work_cn、trae）。而 FR-002a（`PRD.md:98-105`）的 B 族点名含 hermes、`README.md` 源表写 11 个、`src/plugins/` 实有 11 个包。PRD 是**范围**的事实来源，它自己的 In Scope 少一个已经实现并交付的源，等于让照 PRD 点验的人得出"10/10 做完"。**本轮已落实**：`PRD.md` §3.1 采集清单已补 `hermes`，现为 **11 源**。*
- [x] T079 [US6] 把 PRD v0.7.3 的实测回灌 `plan.md` 三处 per plan.md:19,162,192（plan 决策，contradicts）——*三处都停在 v0.7.3 之前：`:19` 写「扩到 11 个源后此预算**未经重新验证**」、`:162` 写「300s 预算只在 4 个源上量过（215.9s），11 个源下不作数」、`:192` 的风险处置写「**逐源计时**并记录」。PRD v0.7.3（`PRD.md:315`）已记：2026-09-26 最忙日 09-25 全源端到端实测 **248.7s / 300s（83%）未突破**，且口径明确订正为**按天计时**（`PRD.md:237`）。与 T065 同源（逐源 vs 按天），只是落点不同：T065 改 PRD/spec 正文，这一条改 plan。**本轮已落实**：`plan.md:19/162/192` 三处回灌 v0.7.3 实测，并把「逐源计时」改为**按天计时**。*
- [x] T080 [P] 订正 `README.md:225` 的用例数 per README.md:225（AC-017，partial）——*写「当前基线（2026-09-25，**548** 用例全绿）」，实测 `pytest --collect-only -q` = **781 tests collected**（2026-09-26）。`README.md:231` 自述这串数字是手写的、无门禁兜底，所以它漂了不会有人被抓。**本轮已落实**：`README.md:230` 改为「**783 用例全绿**」（实测 `pytest --collect-only -q` = 783）。**这条数字无门禁兜底**（`README.md:231` 自述），本轮它又漂了一次：修完是 781，Q6 加固测试加两条后变 783，已一并订正。*
- [x] T081 [P] `README.md:75` 的钩子枚举补齐 `end-of-file-fixer` per README.md:75（AC-017，partial）——*标题「**15** 个钩子」**是对的**（`.pre-commit-config.yaml` 提交期确有 15 个，`mutation-reminder` 挂着 `stages: [post-commit]`，`make gate-selftest` 也按 15 个钩子做红绿配对），但它下面点名的只有 **14** 个，漏掉 `end-of-file-fixer`（与 `trailing-whitespace` 相邻的那一个）。是把"数字对"和"清单全"当成了同一件事。**本轮已落实**：`README.md:75` 钩子枚举补齐 `end-of-file-fixer`，点名的 15 个与标题「15 个钩子」终于对上。*
- [x] T082 [US6] 让 AC-015 的判据可机械验证：`distill_run.per_source`（蒸馏侧 `materials`/`kept`）+ `sync` 汇总的 `skipped_by_source`（入库侧被新颖度拦下）per PRD AC-015（v0.7.9，partial）——*起因是 `qoder` 现场：素材非空 + `distill_run=ok` + 库内 0 点，而「低价值丢弃」与「入库坏了」在观测上分不开（判据要先可判，才谈得上验收）。实现：`src/distill.py:_per_source_counts`（`_finish` 增 `per_source`，四个出口都带）+ `src/ingest.py:_skipped_by_source`（`Report` 增 `skipped_by_source`，默认空 dict 保持向后兼容）+ `src/sync.py` 汇总透出；`data-model.md` 快照形状同步。**留证**：新增 3 条用例（多源日的 per_source 分辨、入库侧 skipped、汇总透出），另更新 3 条钉住快照字面值的既有用例（`set(run) == {model,rubric_hash,status,per_source}`）；`make test` 832 → **834 passed**。门禁：CRAP 241 函数 / 90.7% / 0 crappy、orphans 无孤儿、layers·size·boundary·secret 全 exit 0。**未做**：`gather` 不落盘（→ T083）；`per_source` 只覆盖蒸馏侧 —— 入库侧只能在汇总里，因为快照写在 `ingest` 之前（设计约束，不是遗漏）。*
- [x] T083 [US6] `gather` 不落盘（或把落盘上移到 `sync.run`）per PRD §9 待澄清项第 2 条 / `ac015-method-options.md` §4、§8.4（FR-006，partial）——*v0.7.9 裁决 AC-015 时**未做**：v0.7.8 已拦住最危险的「多源快照被单源覆盖」，剩余缺口是「同数量 + 同来源集合但内容不同」。成本已逐项列在 `ac015-method-options.md` §4（`collect.gather` + `sync.run` 的锁范围 + `distill` 两处 `save_snapshot` 的持久化契约 + 15 处测试调用点 + 写入边界登记项的 `only_from`）。做的前置是批准这次**行为变更**（按仓库惯例走 PRD 变更控制）。收益：采集验证零落盘，顺带得到「只读采集探针」这个可复用能力。**v0.7.10 复核后成本下修**：§4 的五项里三项不用动（`sync.run` 零改动、`distill` 那两处与本变更无关、15 处测试调用点与写入边界门禁都不用改 —— 写入点既没新增也没位移），落地设计见 `ac015-method-options.md` §9；真实成本 = `collect.py` 一个参数 + 只读探针脚本 + 1 条测试 + 一次变异批次重跑。**本轮已落实（2026-09-26，v0.7.11）**：① `src/collect.py:gather` 增 `persist: bool = True`，`if persist:` 包住 `save_snapshot`（生产路径零改动 —— `sync.run` 是唯一生产调用点、走默认值，落盘仍在锁内）；② 新脚本 `scripts/collect_probe.py` + `make probe D=<day> [S=<源>]`，跑 `gather(persist=False)`、逐源计数，**跑前跑后自校快照指纹**（存在性 + 大小/mtime），动了盘就退出 1 —— 这条路径在写入边界门禁的扫描面之外（它扫 `src/`），所以自校是它唯一的机械保证；③ 测试 834 → **864 collected（863 passed + 1 skipped）**：`tests/unit/test_collect.py` 3 条（不写盘 / 已有快照逐字节不变 / 两种模式素材一致）+ `tests/unit/test_collect_probe.py` 12 条（scope 形状、render 合计口径、自校、退出码 0/1/2）；④ `quickstart.md` 的 AC-015 方法段拆成「① 零落盘探针 / ② 必须写盘的 sync」；README 新增「只读采集探针」一节。**实现期实测抓到一个真缺陷**：`--source` 原先只写 `{"tools": …}`，而 git 走 `projects`（缺键 = 启用），于是 `--source qoder` 把 6 条 git 提交算进了 qoder 的合计 —— 改成合计口径只算目标源、其余来源另列并标「未计入合计」（scope 关不掉 `notes/`；实测 2026-09-25：全源 53 条 / qoder 1 条，git 6 条单列）。**门禁**：CRAP 0 crappy、orphans 0、layers·size·boundary·secret 全 exit 0、`gate-selftest` 33/33；变异批次重跑 **1919 杀 / 246 活 / 14 无测试 → 88.6%**（多的那 1 条是 `persist` 默认值），`if persist:` 条件本身 mutmut 不生成变异体，故用手工 oracle 验两向：删守卫 → 3 条红、取反 → 6 条红。*
- [x] T084 [US6] 【已裁定：不是问题】同一次运行**内部**的近似条目算不算需要处置的冗余 per PRD §13 v0.7.10 / `fr007-rerun-options.md` §5.1（FR-007 残留，v0.7.10 登记）——*v0.7.10 量出：全库过 0.82 的 12 条**全部**落在簇内（09-18 两条、09-23 两条、09-25 八条），跨运行一条也没有；09-25 同一次运行内部 max **0.906**、8 条过阈。`filter_novel` 按构造看不见它们（把本批 ID 一律排除，为的是同日重跑仍能到达 upsert），所以**降阈值治不了**，反而先伤跨运行的真条目（只读试算 0.80 → 09-24 拦 7 条 / 09-25 拦 2 条）。**先要回答的是「这算不算问题」**：这些对常常是**同一事件的两种类型**（例：monkeypatch 陷阱在 `error` 与 `progress` 各一条），可能是有意为之的多视角而非冗余。若判为问题，两个候选杠杆都在入库侧 —— ① 每次运行的条数上限（AC-014，今天按运行计、无批内去重）；② 批内近似去重（`ingest` 对本批另做一次阈值比较，与 `filter_novel` 的跨批语义分开）。两者都是行为变更，要按 PRD 变更控制走。**2026-09-26 裁定（v0.7.11 同批，证据见 `fr007-rerun-options.md` §12）：不是问题，不改。** 把 6 对（12 条）逐对打出来之后，三条结构性事实把这条问死了：① 每对的两条**都来自不同素材**（`source_refs` 逐对比过：09-25 那四对是两个不同的 Claude Code 会话各记了同一件事，09-18/09-23 是 Trae 摘要文件里的相邻两条）—— 所以这不是「LLM 把同一条素材蒸馏两遍」，改它等于**按素材删产出**；② in-run 排除是**有意为之且写明了理由**的（`similarity.py:140` 的 `filter_novel` 与 `:186` 的 `build_related_edges` 都排除本批 ID，理由：幂等重跑时本批已在库里、会占掉相似度槽位，不排除则首跑与重跑的邻居不同 → 违反 NFR-003）；③ 代价只有一条**边**（实测这 6 对两两 `related` 全空 —— 它们的最强邻居恰是同批兄弟，最像反而没连上），不是「数据找不回来」（它们本来就可被语义检索取回）。要真动，杠杆在 ① 批内去重 / ② 每次运行条数上限，而 ① 落在 ★ 区（`similarity.py`，用户手写）—— 留待将来按 PRD 变更控制走。*
